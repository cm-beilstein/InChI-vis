---
phase: 08-hydrogen-implicit-explicit-highlight
plan: "00"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/hooks/__tests__/useKetcherHighlights.test.ts
  - src/lib/__tests__/highlightUtils.test.ts
autonomous: true
requirements:
  - INCHI-08

must_haves:
  truths:
    - "renderHBadges and cleanHBadges have failing unit tests that specify the badge injection and cleanup API contract"
    - "The explicit-H bond-lookup path in case 'hAtoms' has a failing unit test that asserts atoms + bond IDs in the spec"
    - "Badge lifecycle (inject on hAtoms subHover, remove on hover-out) has a test that FAILS before implementation"
    - "All 156 existing tests remain green after adding the new failing stubs"
  artifacts:
    - path: "src/hooks/__tests__/useKetcherHighlights.test.ts"
      provides: "renderHBadges + cleanHBadges unit test stubs (RED)"
      contains: "renderHBadges"
    - path: "src/lib/__tests__/highlightUtils.test.ts"
      provides: "case 'hAtoms' explicit-H bond path test stub (RED)"
      contains: "hAtomPoolIds"
  key_links:
    - from: "src/hooks/__tests__/useKetcherHighlights.test.ts"
      to: "src/hooks/useKetcherHighlights.ts"
      via: "import renderHBadges, cleanHBadges"
      pattern: "import.*renderHBadges.*cleanHBadges"
    - from: "src/lib/__tests__/highlightUtils.test.ts"
      to: "src/lib/highlightUtils.ts"
      via: "buildSubHoverSpecs case hAtoms explicit-H path"
      pattern: "hAtomPoolIds.*includes"
---

<objective>
Wave 0 TDD stubs: write RED tests for the three new Phase 8 behaviors before any implementation exists.

Purpose: Establish a failing test baseline that Plan 08-01 turns GREEN. The tests specify the exact API contracts for `renderHBadges`, `cleanHBadges`, and the explicit-H bond-lookup extension to `buildSubHoverSpecs case 'hAtoms'`.
Output: Two test files with new describe blocks that FAIL on `npm test` (import errors or assertion failures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hydrogen-implicit-explicit-highlight/08-CONTEXT.md
@.planning/phases/08-hydrogen-implicit-explicit-highlight/08-RESEARCH.md
@.planning/phases/08-hydrogen-implicit-explicit-highlight/08-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from codebase for executor reference. -->

From src/hooks/__tests__/useKetcherHighlights.test.ts (current imports line 2):
```typescript
import { applyKetcherHighlights, whiteAtomLabels } from '../useKetcherHighlights';
```
Phase 8 adds `renderHBadges` and `cleanHBadges` to this import — they will fail until Plan 08-01 exports them.

From src/hooks/__tests__/useKetcherHighlights.test.ts — existing `makeAtomEl` helper (lines 39–44):
```typescript
function makeAtomEl(atomId: number, label: string): SVGElement {
  const el = document.createElementNS('http://www.w3.org/2000/svg', 'text');
  el.setAttribute('data-atom-id', String(atomId));
  el.setAttribute('data-atomLabel', label);
  return el as unknown as SVGElement;
}
```
Add a new `makeAtomGroup` helper for badge tests (parent `<g>` with mocked `getBBox`).

From src/lib/__tests__/highlightUtils.test.ts — existing `makeMockStruct` (lines 18–33):
```typescript
function makeMockStruct(): StructLike {
  return {
    findBondId: vi.fn().mockReturnValue(99),
    bonds: {
      forEach: vi.fn((cb) => {
        cb({ begin: 0, end: 1 }, 0);
        cb({ begin: 1, end: 2 }, 1);
        // ... 6 bonds total (benzene ring)
      }),
    },
  };
}
```
Add a `makeMockStructWithExplicitH` variant that includes bond 6: `{ begin: 0, end: 6 }` (pool 6 is explicit H bonded to atom 0).

From src/lib/__tests__/highlightUtils.test.ts — existing `buildSubHoverSpecs` test at line 592:
```typescript
it('kind hAtoms: atoms from subHover.atoms[], color from hydroColor(subHover.count)', () => {
  const specs = buildSubHoverSpecs(
    { kind: 'hAtoms', atoms: [1, 2], count: 1 },
    auxMap, atomElements, [], hLayer, makeMockStruct(), resolveVarFn,
  );
  expect(specs[0].atoms).toEqual([0, 1]);
  expect(specs[0].bonds).toHaveLength(0);  // no explicit H → no bonds
```
The new explicit-H test uses `hAtomPoolIds: [6]` and expects `bonds` to contain bond 6.

From src/lib/highlightUtils.ts — SubHover type (via parseInchi.ts):
```typescript
// kind: 'hAtoms' → { kind: 'hAtoms'; atoms: number[]; count: number }
// kind: 'mobileH' → { kind: 'mobileH'; atoms: number[] }
```

From src/lib/__tests__/highlightUtils.test.ts line 9:
```typescript
const resolveVarFn = (name: string): string => name;
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED tests — renderHBadges + cleanHBadges in useKetcherHighlights.test.ts</name>
  <files>src/hooks/__tests__/useKetcherHighlights.test.ts</files>
  <read_first>
    - src/hooks/__tests__/useKetcherHighlights.test.ts — read full file; understand existing imports, makeAtomEl helper, describe structure for whiteAtomLabels (lines 46–86)
    - src/hooks/useKetcherHighlights.ts — confirm current exports (only applyKetcherHighlights + whiteAtomLabels); renderHBadges/cleanHBadges do NOT exist yet
    - .planning/phases/08-hydrogen-implicit-explicit-highlight/08-PATTERNS.md — makeAtomGroup pattern, renderHBadges test cases (section "src/hooks/__tests__/useKetcherHighlights.test.ts")
    - .planning/phases/08-hydrogen-implicit-explicit-highlight/08-RESEARCH.md — Code Examples section, badge attributes spec
  </read_first>
  <behavior>
    - Test 1: renderHBadges injects exactly one `[data-h-badge]` SVG text element per atom in subHover.atoms when atom group exists in SVG
    - Test 2: renderHBadges sets textContent to "H2" for kind:'hAtoms' count:2
    - Test 3: renderHBadges sets textContent to "H?" and font-style="italic" for kind:'mobileH'
    - Test 4: renderHBadges does NOT throw when atom pool ID has no matching DOM element (graceful skip)
    - Test 5: cleanHBadges removes all `[data-h-badge]` elements from the SVG root
    - Test 6: cleanHBadges does not throw on an empty SVG root
  </behavior>
  <action>
    Append two new describe blocks to src/hooks/__tests__/useKetcherHighlights.test.ts.

    First, update the import on line 2 to include `renderHBadges` and `cleanHBadges`:
    `import { applyKetcherHighlights, whiteAtomLabels, renderHBadges, cleanHBadges } from '../useKetcherHighlights';`

    Also add the SubHover type import: `import type { SubHover } from '../../lib/parseInchi';`

    Add `makeAtomGroup` helper after the existing `makeAtomEl` function. The group must contain a `<text data-atom-id="N">` child, and mock `getBBox` on the group element returning `{ x: X, y: Y, width: 20, height: 16 }`. JSDOM does not implement `getBBox` natively — cast the group and assign `(g as any).getBBox = () => ({...})`.

    Write `describe('renderHBadges', () => { ... })` with the six test cases from the behavior block above. Use `makeAtomGroup(0, 100, 50)` appended to an SVG element as the DOM fixture. The `auxMap` argument maps canonical atom index to pool ID (e.g. `{ 1: 0 }` for canonical 1 → pool 0). The `resolveVarFn` argument is `(name: string) => name` (identity, same as highlightUtils tests).

    Write `describe('cleanHBadges', () => { ... })` testing: (a) removes all [data-h-badge] elements from an SVG with two badge elements, (b) does not throw on empty SVG.

    These tests MUST fail at this stage because `renderHBadges` and `cleanHBadges` are not yet exported from `useKetcherHighlights.ts`. The import line will cause a TypeScript/Vitest error — this is the expected RED state. Do NOT add any implementation.
  </action>
  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` output shows the new `describe('renderHBadges')` and `describe('cleanHBadges')` tests failing (import error or assertion failure)
    - The 156 existing tests remain green (only new tests fail)
    - Import line in useKetcherHighlights.test.ts contains `renderHBadges` and `cleanHBadges`
    - `makeAtomGroup` helper is present with `getBBox` mock
    - `describe('renderHBadges')` block has at least 4 `it(...)` cases
    - `describe('cleanHBadges')` block has at least 2 `it(...)` cases
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: RED tests — case 'hAtoms' explicit-H bond path in highlightUtils.test.ts</name>
  <files>src/lib/__tests__/highlightUtils.test.ts</files>
  <read_first>
    - src/lib/__tests__/highlightUtils.test.ts — read from line 490 onward; understand existing buildSubHoverSpecs describe block, existing hAtoms tests at lines 592–640, makeMockStruct structure
    - src/lib/highlightUtils.ts lines 314–322 — current case 'hAtoms' implementation (atom-only, no bond IDs)
    - .planning/phases/08-hydrogen-implicit-explicit-highlight/08-PATTERNS.md — makeMockStructWithExplicitH fixture and explicit-H test case patterns (section "src/lib/__tests__/highlightUtils.test.ts")
  </read_first>
  <behavior>
    - Test 1: case 'hAtoms' with pool ID NOT in hAtomPoolIds (implicit H) → spec.atoms includes heavy atom pool ID, spec.bonds is empty (this is a regression test — current behavior is correct and must remain passing)
    - Test 2: case 'hAtoms' with pool ID IN hAtomPoolIds (explicit H) → spec.atoms includes the explicit H pool ID AND the bonded heavy atom pool ID; spec.bonds includes the bond ID between them
    - Test 3: case 'hAtoms' with mixed atoms (some explicit H, some implicit) → spec.atoms includes all atoms; spec.bonds includes only the explicit-H bond IDs
  </behavior>
  <action>
    Add `makeMockStructWithExplicitH()` helper to src/lib/__tests__/highlightUtils.test.ts. This function returns a `StructLike` whose `bonds.forEach` iterates 7 bonds: bonds 0–5 are the existing benzene ring (same as `makeMockStruct`), plus bond 6: `cb({ begin: 0, end: 6 }, 6)` representing explicit H (pool 6) bonded to heavy atom 0.

    Inside the existing `describe('buildSubHoverSpecs', () => { ... })` block, append a new nested describe: `describe('case hAtoms — Phase 8 explicit-H bond path', () => { ... })` with the three test cases from the behavior block.

    Test 2 setup: `buildSubHoverSpecs({ kind: 'hAtoms', atoms: [7], count: 1 }, { 7: 6 }, {}, [6], hLayer, makeMockStructWithExplicitH(), resolveVarFn)` — canonical 7 maps to pool 6; pool 6 is in hAtomPoolIds; bond 6 connects pool 6 to pool 0; expect `spec.atoms` to contain both 6 (explicit H) and 0 (heavy atom); expect `spec.bonds` to contain 6.

    Test 3 setup: subHover has atoms [1, 7]; auxMap maps 1→0 (implicit H, pool 0 not in hAtomPoolIds), 7→6 (explicit H, pool 6 in hAtomPoolIds); expect spec.atoms to contain 0, 6; spec.bonds to contain 6 but NOT 0 or 1.

    Tests 2 and 3 MUST FAIL at this stage (current case 'hAtoms' returns bonds: []). Test 1 is a regression assertion that must PASS immediately.
  </action>
  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npm test -- --reporter=verbose 2>&1 | grep -A5 "explicit-H bond path"</automated>
  </verify>
  <acceptance_criteria>
    - `describe('case hAtoms — Phase 8 explicit-H bond path')` block is present in highlightUtils.test.ts
    - `makeMockStructWithExplicitH` function is present with bond 6: `{ begin: 0, end: 6 }`
    - Test 1 (regression: no bonds for implicit H) PASSES (current implementation already satisfies it)
    - Tests 2 and 3 (explicit-H bond path) FAIL (bonds: [] assertion fails because current implementation returns bonds: [])
    - All other existing 156 tests remain green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production imports | Tests import non-existent exports — expected RED state; no security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 8 |
</threat_model>

<verification>
After Wave 0 completes:
- `npm test` shows 156 existing tests PASS
- 5–8 new tests FAIL with import or assertion errors
- No TypeScript errors in existing source files (only the test file import fails)
- `grep "renderHBadges\|cleanHBadges" src/hooks/__tests__/useKetcherHighlights.test.ts` returns matches
- `grep "explicit-H bond path\|makeMockStructWithExplicitH" src/lib/__tests__/highlightUtils.test.ts` returns matches
</verification>

<success_criteria>
Wave 0 is complete when:
1. New test cases for renderHBadges and cleanHBadges exist and FAIL (import error)
2. New test cases for explicit-H bond path in case 'hAtoms' exist and FAIL (assertion error)
3. All 156 pre-existing tests remain green
4. No changes to any production source file
</success_criteria>

<output>
Create `.planning/phases/08-hydrogen-implicit-explicit-highlight/08-00-SUMMARY.md` when done
</output>
