---
context: phase
phase: 08-hydrogen-implicit-explicit-highlight
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-06-05T11:53:05Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Assuming JSDOM badge tests validate visual placement | `makeAtomGroup` mocks `getBBox()` with fixed coords — tests confirm DOM injection but not real Ketcher SVG positioning. Phase verification listed this as "Assumption A1 — empirical verify needed". | advisory | Always browser-test badge placement on ring molecules after any positioning change |

<current_state>
Phase 08 is code-complete. All 165 tests pass. A post-completion bug was found and fixed this session: implicit H badges were colliding with bonds and appearing inside ring interiors.

**Fix is committed to the working tree but NOT yet committed to git.**

`src/hooks/useKetcherHighlights.ts` has the fix applied but is still an uncommitted modification.
</current_state>

<completed_work>

- **Task 1 (done):** Identified root cause (fixed +20 downward SVG offset → badges land inside ring interiors for upper atoms) and applied fix:
  - `renderHBadges` rewritten to use two-pass radial offset from centroid (18px outward)
  - Added white stroke halo (`stroke="white" stroke-width="3" paint-order="stroke fill"`) for bond-line readability
  - All 165 tests still pass
</completed_work>

<remaining_work>

- **Task 2 (not started):** Browser verification — hover h-layer sub-tokens on a ring molecule (e.g., benzene preset) and confirm:
  1. Badges appear OUTSIDE the ring perimeter, not inside or on bond lines
  2. Adjacent atom badges don't collide
  3. Single-atom molecules: badge appears above the atom (fallback to -y)
  4. Mobile-H group: italic "H?" badge, same radial placement

- **After verification passes:** commit `src/hooks/useKetcherHighlights.ts` with message `fix(08): radial badge offset to prevent ring-interior placement`
- **Then:** run `/gsd-complete-milestone` to archive v1.0
</remaining_work>

<decisions_made>

- **Radial offset (18px from centroid)** instead of fixed +20 downward — fixed downward always goes INTO rings for upper atoms; radial pushes outward regardless of ring orientation
- **White stroke halo** (`stroke-width=3`, `paint-order=stroke fill`) — ensures badge text is readable even if it grazes a bond line in tight geometries
- **Single-atom fallback: offset upward (-y)** — when centroid == atom center (len < 1), fall back to straight up since there's no ring to point away from
</decisions_made>

<blockers>
None — fix applied, just needs browser sign-off.
</blockers>

## Required Reading (in order)
1. `src/hooks/useKetcherHighlights.ts` — `renderHBadges` function (lines ~66–107) — see the two-pass radial offset implementation
2. `.planning/phases/08-hydrogen-implicit-explicit-highlight/08-VERIFICATION.md` — "Assumption A1" section — documents the original getBBox risk

## Infrastructure State
- Dev server: already running (`npm run dev`)
- All 165 tests: passing

<context>
Phase 08 was marked complete but the badge positioning assumption (A1) was flagged as needing empirical verification. On browser test this session, badges were appearing but landed inside ring interiors and on bond lines due to the fixed +20 downward offset.

The radial offset approach is the right fix: it uses the geometry of the molecule itself (centroid of all highlighted atoms) to determine the outward direction for each badge. For benzene, all 6 badges should point outward from the ring center. For a linear chain, they point away from the chain midpoint (mostly upward/downward for each end atom, perpendicular for middle atoms).
</context>

<next_action>
1. Open browser at localhost (dev server running)
2. Draw benzene or load a preset ring molecule
3. Hover h-layer sub-tokens — verify badges appear OUTSIDE the ring
4. If OK: `git add src/hooks/useKetcherHighlights.ts && git commit -m "fix(08): radial badge offset to prevent ring-interior placement"`
5. Then: `/gsd-complete-milestone`
</next_action>
