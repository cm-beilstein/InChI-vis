---
phase: 06-hydrogen-highlight-polish-and-deploy
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/__tests__/InchiSection.test.tsx
  - src/lib/__tests__/highlightUtils.test.ts
autonomous: true
requirements:
  - INCHI-05
  - PLSH-01
must_haves:
  truths:
    - "src/__tests__/InchiSection.test.tsx exists and its tests run RED (failing) until InchiSection returns the dimmed box"
    - "highlightUtils.test.ts contains RED tests for H-branch in buildSubHoverSpecs (el='H', hAtomPoolIds provided/empty)"
    - "highlightUtils.test.ts contains RED tests for formula-layer buildHighlightSpecs with hAtomPoolIds appended"
    - "npm test -- --run exits non-zero (RED state confirms test stubs are valid and compiling)"
  artifacts:
    - path: "src/__tests__/InchiSection.test.tsx"
      provides: "PLSH-01 test coverage — empty state render, placeholder text, no layout shift"
      exports: []
    - path: "src/lib/__tests__/highlightUtils.test.ts"
      provides: "INCHI-05 H-branch test cases appended to existing 29-test suite"
      contains: "hAtomPoolIds"
  key_links:
    - from: "src/__tests__/InchiSection.test.tsx"
      to: "src/components/InchiSection.tsx"
      via: "React Testing Library render"
      pattern: "import.*InchiSection"
    - from: "src/lib/__tests__/highlightUtils.test.ts"
      to: "src/lib/highlightUtils.ts"
      via: "buildSubHoverSpecs / buildHighlightSpecs calls with hAtomPoolIds param"
      pattern: "hAtomPoolIds"
---

<objective>
Wave 0 test stubs for Phase 6. Creates failing (RED) tests that define the acceptance contract for PLSH-01 (InchiSection empty state) and INCHI-05 (H sub-token highlight). Implementation plans (06-01 and 06-02) turn these tests GREEN.

Purpose: Enforce TDD discipline — tests define the contract before a single production line changes. Wave 1 plans cannot be marked done until these tests pass.
Output: Two test files with all Phase 6 unit test cases in RED state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-hydrogen-highlight-polish-and-deploy/06-CONTEXT.md
@.planning/phases/06-hydrogen-highlight-polish-and-deploy/06-RESEARCH.md

<interfaces>
<!-- Key signatures the test stubs must reference. Extracted from current source. -->

From src/lib/highlightUtils.ts (current signatures — Wave 1 will extend these):
```typescript
// CURRENT (to be extended in 06-01):
export function buildHighlightSpecs(
  layer: Layer,
  subHover: SubHover | null,
  auxMap: AuxMap,
  atomElements: Record<number, string>,
  layers: Layer[],
  struct: StructLike,
  resolveVarFn: (name: string) => string,
): HighlightSpec[]

export function buildSubHoverSpecs(
  subHover: SubHover,
  auxMap: AuxMap,
  atomElements: Record<number, string>,
  layer: Layer,
  struct: StructLike,
  resolveVarFn: (name: string) => string,
): HighlightSpec[]
// AFTER 06-01 both functions gain a new hAtomPoolIds: number[] parameter
// inserted AFTER atomElements and BEFORE layers (buildHighlightSpecs)
// or AFTER atomElements and BEFORE layer (buildSubHoverSpecs)
```

From src/store.ts (current interface — Wave 1 will add hAtomPoolIds field):
```typescript
interface InchiState {
  inchi: string;
  layers: Layer[];
  auxMap: AuxMap;
  atomElements: Record<number, string>;
  hoverIdx: number | null;
  subHover: SubHover | null;
  setInchiData: (inchi: string, layers: Layer[], auxMap: AuxMap, atomElements: Record<number, string>) => void;
  // AFTER 06-01: setInchiData gains 5th param: hAtomPoolIds: number[]
}
```

From src/components/InchiSection.tsx (current behavior — Wave 1 will change):
```typescript
// CURRENT: returns null when layers is empty
if (layers.length === 0) return null;
// AFTER 06-02: renders dimmed box with placeholder text
```

Existing test file structure at src/lib/__tests__/highlightUtils.test.ts:
- Lines 1-139: fixtures (benzene auxMap, atomElements, makeLayer, layer constants)
- Lines 140+: describe('buildHighlightSpecs') and describe('buildSubHoverSpecs') suites
- The file has 29 tests currently passing
- NEW tests for H branch are APPENDED at the end of the file
- Key fixture needed: hAtomPoolIds = [10, 11, 12] (3 explicit H pool IDs, arbitrary numbers that do NOT appear in auxMap)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: InchiSection test stubs (RED — PLSH-01)</name>
  <files>src/__tests__/InchiSection.test.tsx</files>
  <read_first>
    - src/components/InchiSection.tsx (current component — understand what it renders and where early-return null is)
    - src/__tests__/MappingStrip.test.tsx (existing React Testing Library pattern used in this project — replicate setup)
    - src/__tests__/store.test.ts (how store is reset in beforeEach)
  </read_first>
  <behavior>
    Write a new test file src/__tests__/InchiSection.test.tsx with the following test cases. All tests MUST be in RED (failing) against the current src/components/InchiSection.tsx which returns null when layers is empty.

    Test setup:
    - Framework: Vitest + React Testing Library (@testing-library/react, @testing-library/jest-dom)
    - Mock useInchiStore with vi.mock('../store') — control layers[] and hoverIdx values
    - In beforeEach, reset store mock to return { layers: [], hoverIdx: null, setHover: vi.fn(), setSubHover: vi.fn() }

    Test cases (these are the acceptance contract — do NOT modify them during execution):

    describe('InchiSection — PLSH-01 empty state'):
      it('renders the inchi-display box even when layers is empty'):
        - Mock store: layers = [], hoverIdx = null
        - render(<InchiSection />)
        - Query for a container with data-testid or by role — the dimmed box must be present
        - Assertion: expect(container.querySelector('[data-empty="true"]')).toBeInTheDocument()
        - RED signal: current code returns null, so this fails

      it('shows the exact placeholder text when layers is empty'):
        - Mock store: layers = [], hoverIdx = null
        - render(<InchiSection />)
        - Assertion: expect(screen.getByText('Draw a molecule above to see its InChI.')).toBeInTheDocument()
        - RED signal: current code returns null

      it('does NOT render InChI= prefix when layers is empty'):
        - Mock store: layers = [], hoverIdx = null
        - render(<InchiSection />)
        - Assertion: expect(screen.queryByText(/InChI=/)).not.toBeInTheDocument()
        - RED signal: current code returns null (paradoxically passes — keep this test, it must STILL pass after fix)
        Note: This test passes in RED state (returns null, so no InChI= prefix). It must continue to pass after the fix.

      it('renders InChI= prefix and layers when layers is non-empty'):
        - Mock store: layers = [{ type: 'formula', prefix: '', text: 'C6H6', atoms: [1,2,3,4,5,6], bonds: [] }], hoverIdx = null
        - render(<InchiSection />)
        - Assertion: expect(screen.getByText(/InChI=/)).toBeInTheDocument()
        - RED signal: this may already pass — include it as a regression test

      it('does not apply data-empty="true" when layers is non-empty'):
        - Mock store: layers = [{ type: 'formula', prefix: '', text: 'C6H6', atoms: [], bonds: [] }], hoverIdx = null
        - render(<InchiSection />)
        - Assertion: expect(container.querySelector('[data-empty="true"]')).not.toBeInTheDocument()

    After writing, run: npm test -- --run src/__tests__/InchiSection.test.tsx
    The 'renders the inchi-display box even when layers is empty' and 'shows the exact placeholder text' tests MUST fail (RED). If any fail due to a setup error (import error, mock error), fix the setup before proceeding — RED means the assertion fails, not the test crashes.
  </behavior>
  <action>
    Create src/__tests__/InchiSection.test.tsx.

    Use @testing-library/react (already in devDependencies from Phase 3). Check package.json to confirm. If not present, add: npm install --save-dev @testing-library/react @testing-library/jest-dom.

    The vitest.config.ts uses environment: 'jsdom' for component tests — confirm this is already set (it is, per Phase 3). Do not modify vitest.config.ts.

    Mock pattern for useInchiStore (replicate from MappingStrip.test.tsx or store.test.ts):
    ```typescript
    import { vi } from 'vitest';
    vi.mock('../store', () => ({
      useInchiStore: vi.fn((selector) => selector({
        layers: mockLayers,
        hoverIdx: mockHoverIdx,
        setHover: vi.fn(),
        setSubHover: vi.fn(),
      })),
    }));
    ```
    Use a `let mockLayers: Layer[] = []` and `let mockHoverIdx: number | null = null` that are reset in beforeEach.

    Exact placeholder text the test must match: 'Draw a molecule above to see its InChI.'
    Exact data attribute to query: [data-empty="true"]

    Import path: `../store` (test is in src/__tests__/, InchiSection is in src/components/)
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/InchiSection.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/__tests__/InchiSection.test.tsx exists
    - File imports InchiSection from '../components/InchiSection'
    - File contains the string 'Draw a molecule above to see its InChI.'
    - File contains the string 'data-empty'
    - npm test run shows at least 2 FAILING tests in this file (the empty-state render tests)
    - No test crashes due to missing imports — all imports resolve (setup errors are not RED, they are broken)
  </acceptance_criteria>
  <done>src/__tests__/InchiSection.test.tsx exists with 5 test cases; at minimum 2 are RED (assertion failures, not setup errors); npm test exits non-zero for this file</done>
</task>

<task type="tdd">
  <name>Task 2: highlightUtils H-branch test cases (RED — INCHI-05)</name>
  <files>src/lib/__tests__/highlightUtils.test.ts</files>
  <read_first>
    - src/lib/__tests__/highlightUtils.test.ts (read entire file — append at end, preserve all 29 existing tests)
    - src/lib/highlightUtils.ts (current signatures — understand current buildSubHoverSpecs case 'element' and buildHighlightSpecs formula case)
    - .planning/phases/06-hydrogen-highlight-polish-and-deploy/06-RESEARCH.md (Pattern 1 code examples — exact function signatures after 06-01 changes)
  </read_first>
  <behavior>
    APPEND new test cases to the end of src/lib/__tests__/highlightUtils.test.ts. Do NOT modify any existing tests. Do NOT modify existing imports.

    Add a new top-level describe block: describe('INCHI-05: H sub-token highlight')

    New fixture to define before the describe block:
    ```typescript
    // Explicit H pool IDs — arbitrary values not in auxMap
    const hAtomPoolIds = [10, 11, 12];
    const emptyHAtomPoolIds: number[] = [];
    ```

    Test cases inside describe('INCHI-05: H sub-token highlight'):

      describe('buildSubHoverSpecs — element H branch'):

        it('returns hAtomPoolIds as atoms when el is H and hAtomPoolIds is non-empty'):
          - Input: subHover = { kind: 'element', el: 'H' }, hAtomPoolIds = [10, 11, 12]
          - Call: buildSubHoverSpecs(subHover, auxMap, atomElements, hAtomPoolIds, formulaLayer, makeMockStruct(), resolveVarFn)
          NOTE: After 06-01, the new signature is:
          buildSubHoverSpecs(subHover, auxMap, atomElements, hAtomPoolIds, layer, struct, resolveVarFn)
          The hAtomPoolIds param is inserted AFTER atomElements.
          - Assertion: expect(specs).toHaveLength(1)
          - Assertion: expect(specs[0].atoms).toEqual([10, 11, 12])
          - Assertion: expect(specs[0].bonds).toEqual([])
          - Assertion: expect(specs[0].color).toBe('--c-el-H')  // identity resolveVarFn returns the var name
          - RED signal: current signature has no hAtomPoolIds param, call will fail to compile or return wrong result

        it('returns empty array (silent no-op) when el is H and hAtomPoolIds is empty'):
          - Input: subHover = { kind: 'element', el: 'H' }, hAtomPoolIds = []
          - Call: buildSubHoverSpecs(subHover, auxMap, atomElements, emptyHAtomPoolIds, formulaLayer, makeMockStruct(), resolveVarFn)
          - Assertion: expect(specs).toEqual([])
          - RED signal: current code looks up H in atomElements/auxMap (no H entries exist) and returns [] — BUT this test will fail because the function signature doesn't accept hAtomPoolIds yet

        it('non-H element sub-hover still works with hAtomPoolIds param (regression)'):
          - Input: subHover = { kind: 'element', el: 'C' }, hAtomPoolIds = []
          - Call: buildSubHoverSpecs(subHover, auxMap, atomElements, emptyHAtomPoolIds, formulaLayer, makeMockStruct(), resolveVarFn)
          - Assertion: expect(specs[0].atoms).toContain(0)  // canonical 1 → ketcher 0
          - Assertion: expect(specs[0].color).toBe('--c-el-C')
          - RED signal: signature mismatch until 06-01

      describe('buildHighlightSpecs formula layer — H pool IDs appended'):

        it('formula layer with non-empty hAtomPoolIds includes H pool IDs in specs'):
          - Input: formulaLayer (C6H6), hAtomPoolIds = [10, 11, 12]
          - Call: buildHighlightSpecs(formulaLayer, null, auxMap, atomElements, hAtomPoolIds, allLayers, makeMockStruct(), resolveVarFn)
          NOTE: After 06-01, the new signature is:
          buildHighlightSpecs(layer, subHover, auxMap, atomElements, hAtomPoolIds, layers, struct, resolveVarFn)
          The hAtomPoolIds param is inserted AFTER atomElements.
          - Assertion: const allAtoms = specs.flatMap(s => s.atoms); expect(allAtoms).toContain(10)
          - Assertion: expect(allAtoms).toContain(11)
          - Assertion: expect(allAtoms).toContain(12)
          - RED signal: current signature has no hAtomPoolIds param

        it('formula layer with empty hAtomPoolIds does not include phantom atoms in specs'):
          - Input: formulaLayer (C6H6), hAtomPoolIds = []
          - Call: buildHighlightSpecs(formulaLayer, null, auxMap, atomElements, emptyHAtomPoolIds, allLayers, makeMockStruct(), resolveVarFn)
          - Assertion: const allAtoms = specs.flatMap(s => s.atoms); expect(allAtoms).not.toContain(10)
          - Assertion: expect(allAtoms).not.toContain(11)
          - RED signal: signature mismatch until 06-01

    CRITICAL: All existing tests in this file call buildHighlightSpecs and buildSubHoverSpecs with the OLD signatures (without hAtomPoolIds). After 06-01 adds the new parameter, those existing calls will break. To prevent that:
    - In Task 2 (this task), do NOT change any existing test calls.
    - In 06-01 Task 1, when updating the function signatures, ensure hAtomPoolIds defaults to [] so existing callers don't break: hAtomPoolIds: number[] = []
    - The existing 29 tests must still pass after 06-01.

    After writing, run: npm test -- --run src/lib/__tests__/highlightUtils.test.ts
    The new H-branch tests MUST fail (RED). Existing 29 tests must still pass.
  </behavior>
  <action>
    Append to the end of src/lib/__tests__/highlightUtils.test.ts — do not insert, do not modify existing content.

    The new describe block calls buildSubHoverSpecs and buildHighlightSpecs with the POST-06-01 signatures (with hAtomPoolIds param). These calls will fail to type-check (TypeScript error) or return wrong values against the CURRENT implementation — that is the RED state.

    If TypeScript strict mode causes compilation errors at test collection time (preventing other tests from running), add // @ts-expect-error above the failing calls in the RED state. This is acceptable for Wave 0 stub tests — they will be fixed when 06-01 updates the function signatures.

    Exact import paths are already in the file — no new imports needed.
    The formulaLayer, auxMap, atomElements, makeMockStruct, resolveVarFn fixtures are already defined in the file.
    The allLayers array is already defined in the file.
    Only the hAtomPoolIds fixture and the new describe block need to be added.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/__tests__/highlightUtils.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/__tests__/highlightUtils.test.ts contains the string 'hAtomPoolIds'
    - src/lib/__tests__/highlightUtils.test.ts contains the string 'INCHI-05'
    - src/lib/__tests__/highlightUtils.test.ts contains the string 'silent no-op'
    - npm test run shows the existing 29 tests still passing (no regressions in existing tests)
    - npm test run shows at least 5 new tests listed (RED or setup errors acceptable for new tests)
    - The file total line count is greater than the pre-task count (new content was appended)
  </acceptance_criteria>
  <done>highlightUtils.test.ts has new H-branch tests appended; existing 29 tests are GREEN; new H-branch tests are RED or skipped; npm test exits non-zero (due to RED tests)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Source | Test files import and call production source; no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-W0-01 | Tampering | test fixtures | accept | Test files are dev-only; no prod surface area |

This plan creates only test files. No production code changes, no trust boundary crossings, no user data, no network calls.
</threat_model>

<verification>
After both tasks complete:
- npm test -- --run exits non-zero (RED state is correct)
- Both test files exist
- Existing 106 tests from prior phases are unaffected (grep count on passing tests ≥ 106 minus any that are now broken by the new type-error stubs)
</verification>

<success_criteria>
Wave 0 complete when:
- src/__tests__/InchiSection.test.tsx exists with ≥5 tests, ≥2 RED
- src/lib/__tests__/highlightUtils.test.ts has hAtomPoolIds describe block appended, ≥5 new RED tests
- Existing ≥106 tests still pass (no regressions in prior-phase tests)
- npm test -- --run exits non-zero
</success_criteria>

<output>
After completion, create `.planning/phases/06-hydrogen-highlight-polish-and-deploy/06-00-SUMMARY.md`
</output>
