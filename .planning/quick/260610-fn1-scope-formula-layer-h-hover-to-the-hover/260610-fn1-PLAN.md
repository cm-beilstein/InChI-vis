---
phase: quick-260610-fn1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/highlightUtils.ts
  - src/lib/__tests__/highlightUtils.test.ts
autonomous: true
requirements: [QUICK-260610-fn1]
must_haves:
  truths:
    - "Hovering one fragment's H in a multi-fragment formula highlights only that fragment's explicit H atoms"
    - "Hovering H with no canonRange still highlights all explicit H atoms (single-fragment unchanged)"
    - "Empty hAtomPoolIds is a silent no-op (returns [])"
  artifacts:
    - path: "src/lib/highlightUtils.ts"
      provides: "canonRange-scoped el==='H' branch in buildSubHoverSpecs"
      contains: "poolToCanon"
    - path: "src/lib/__tests__/highlightUtils.test.ts"
      provides: "regression tests for fragment-scoped H hover"
  key_links:
    - from: "buildSubHoverSpecs el==='H' branch"
      to: "subHover.canonRange + struct.bonds + auxMap"
      via: "reverse auxMap (poolToCanon) + bonded-neighbor lookup"
      pattern: "poolToCanon"
---

<objective>
Fix the `el === 'H'` branch in `buildSubHoverSpecs` so explicit-H hover respects `subHover.canonRange`. Currently it returns the full `hAtomPoolIds` list regardless of which fragment is hovered, so in a dot-separated (multi-fragment) formula, hovering one fragment's H highlights explicit H atoms across ALL fragments.

Purpose: Match the existing fragment-scoping behavior already correct for non-H elements (C/N) in the same branch.
Output: Scoped H highlighting + TDD regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/lib/highlightUtils.ts
@src/lib/__tests__/highlightUtils.test.ts

<interfaces>
From src/lib/highlightUtils.ts:
```typescript
export interface StructLike {
  findBondId(begin: number, end: number): number | null;
  bonds: { forEach(cb: (bond: { begin: number; end: number }, id: number) => void): void };
  atoms: { forEach(cb: (atom: { charge?: number | null }, id: number) => void): void };
}
```

Current el==='H' branch (src/lib/highlightUtils.ts ~lines 386-390):
```typescript
if (el === 'H') {
  if (hAtomPoolIds.length === 0) return []; // silent no-op per D-04
  const color = resolveVarFn(stripVar(elementColor('H')));
  return [{ atoms: hAtomPoolIds, bonds: [], rgroupAttachmentPoints: [], color }];
}
```

`auxMap` maps `canonical -> poolId`. `hAtomPoolIds` are H pool IDs. `struct.bonds.forEach` yields `{begin, end}` pool-ID endpoints.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing regression tests for fragment-scoped H hover</name>
  <files>src/lib/__tests__/highlightUtils.test.ts</files>
  <behavior>
    Fixture (2 fragments): auxMap {1:10, 2:20}, atomElements {1:'C', 2:'C'},
    hAtomPoolIds [100,200], struct.bonds yields {begin:10,end:100} and {begin:20,end:200}
    (struct.findBondId ()=>null, struct.atoms.forEach noop),
    layer = makeLayer({ type:'formula', atoms:[1,2], bonds:[] }), resolveVarFn = (n)=>n.
    Invoke: buildSubHoverSpecs(sub, auxMap, atomElements, hAtomPoolIds, layer, struct, rv),
    then flatten returned specs' `atoms`.
    - {kind:'element',el:'H',canonRange:[2,2]} -> flattened atoms === [200]
    - {kind:'element',el:'H',canonRange:[1,1]} -> [100]
    - {kind:'element',el:'H'} (no canonRange) -> [100,200] (single-fragment behavior unchanged)
    - {kind:'element',el:'H',canonRange:[1,2]} -> [100,200] (both in range)
    - hAtomPoolIds=[] with canonRange:[2,2] -> [] (no-op)
  </behavior>
  <action>Add a new `describe('QUICK-260610-fn1: fragment-scoped explicit-H hover', ...)` block to src/lib/__tests__/highlightUtils.test.ts. Build a local mock StructLike whose `bonds.forEach` calls `cb({begin:10,end:100},0)` then `cb({begin:20,end:200},1)`, `findBondId: () => null`, `atoms.forEach: vi.fn()`. Use the existing `makeLayer` helper for the formula layer. Match the existing test file style (`it('...')`, `expect(...).toEqual(...)`). Flatten via `specs.flatMap(s => s.atoms)`. These tests MUST fail against the current implementation (the canonRange cases will return [100,200] instead of the scoped subset).</action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/highlightUtils.test.ts -t "fragment-scoped explicit-H" 2>&1 | tail -20</automated>
  </verify>
  <done>New tests exist; canonRange cases FAIL (RED) against unmodified highlightUtils.ts, confirming the bug is reproduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope el==='H' branch to canonRange via bonded-neighbor lookup</name>
  <files>src/lib/highlightUtils.ts</files>
  <action>Modify ONLY the `if (el === 'H')` branch in `buildSubHoverSpecs`. Keep the leading `if (hAtomPoolIds.length === 0) return [];` guard. If `subHover.canonRange === undefined`, keep current behavior exactly (return all hAtomPoolIds). If `subHover.canonRange` is `[lo, hi]`: build a reverse map `poolToCanon` by inverting auxMap (`for (const [cStr, pid] of Object.entries(auxMap)) poolToCanon.set(pid, Number(cStr))`). For each H poolId in hAtomPoolIds, find its bonded neighbor by iterating `struct.bonds.forEach` — the endpoint (begin/end) that is not this H poolId; skip a bond if neither endpoint equals this H; defensively skip if the neighbor is itself in hAtomPoolIds (H-H). Look up neighbor's canonical via poolToCanon; keep this H if that canonical is `>= lo && <= hi`. After filtering, if the result is empty return []. Color resolution unchanged: `resolveVarFn(stripVar(elementColor('H')))`. Do NOT touch the non-H part of the branch or any other case/file. NEVER reconstruct InChI — this is highlight mapping only.</action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <done>tsc -b clean; full vitest suite (197 existing + new fragment-scoped tests) passes GREEN. canonRange:[2,2]->[200], [1,1]->[100], undefined->[100,200], [1,2]->[100,200], empty pool->[].</done>
</task>

</tasks>

<verification>
- `npx tsc -b` exits 0 with no errors.
- `npx vitest run` passes all tests including the 5 new fragment-scoped assertions.
- Only src/lib/highlightUtils.ts and src/lib/__tests__/highlightUtils.test.ts changed (`git diff --name-only`).
</verification>

<success_criteria>
- Fragment-scoped H hover returns only the hovered fragment's explicit H pool IDs when canonRange is defined.
- No-canonRange and empty-pool behaviors unchanged.
- Full test suite green, tsc clean.
</success_criteria>

<output>
Create `.planning/quick/260610-fn1-scope-formula-layer-h-hover-to-the-hover/260610-fn1-SUMMARY.md` when done.
</output>
