---
context: default
phase: null
task: null
total_tasks: null
status: blocked
last_updated: 2026-06-08T11:50:54Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered through failure this session._

<current_state>
Ad-hoc debugging session. Ketcher menus (context menus, toolbar dropdowns, dialog windows)
reported as hidden. All overflow:hidden removed from our CSS wrapper layers. Cause unknown —
browser DevTools inspection required before continuing.

All changes committed (a123ecb and earlier). Working tree is clean.
</current_state>

<completed_work>

- **commit 60c6000** — Removed canvas-meta overlay (formula + heavy atom count in Ketcher top-left)
- **commit 2b4c84c** — Removed overflow:hidden from global .ketcher (dead CSS — no element has literal class 'ketcher'; Ketcher root is 'Ketcher-root')
- **commit a123ecb** — Removed overflow:hidden from global .canvas-wrap; cleaned module .canvasWrap override and stale comments
</completed_work>

<remaining_work>

- **BLOCKED**: Diagnose why Ketcher menus still hidden after removing all our overflow:hidden
- Fix once root cause identified

**Required human action before resuming:**
Open browser DevTools, right-click inside Ketcher canvas to trigger context menu, inspect
element with class `ContextMenu-module_contextMenu__oqyL-` and report:
- computed `top`, `left` (off-screen?)
- computed `z-index` (1 or something else?)
- computed `display`, `visibility`, `opacity`
- whether element exists in DOM at all
</remaining_work>

<decisions_made>

- **Global .ketcher CSS is dead code**: No element has literal class `ketcher`. Ketcher root
  uses `Ketcher-root` (capital K). Confirmed in bundle: `KETCHER_ROOT_NODE_CLASS_NAME = 'Ketcher-root'`.

- **Ketcher menu positioning**:
  - Toolbar dropdown portals: `position:absolute; z-index:20` appended to `.Ketcher-root`
  - Context menu: `position:fixed; z-index:1` (very low — suspect)
  - Modal overlays: `position:absolute; z-index:40` within `.Ketcher-root`

- **container-type:size does NOT clip overflow**: `App-module_app__8cgyF` uses
  `container-type:size` = `contain:size layout style` (NOT paint). Only paint containment
  clips overflow. Not the cause.
</decisions_made>

<blockers>

- Root cause unknown. Hypotheses: (1) context menu z-index:1 too low for page stacking
  context, (2) portal coordinates wrong, (3) something in Ketcher's own CSS not yet found.
  Cannot narrow further without live browser DevTools data.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full decision log with Ketcher class names

## Infrastructure State
- Dev server: likely running on port 5173
- Tests: 174/174 passing
- Working tree: clean

<context>
User reported canvas-meta as unwanted overlay — fixed. Then reported Ketcher menus
cut off at borders. Removed overflow:hidden at multiple levels. Issue persists.
Deep analysis of Ketcher 3.12.0 bundle revealed menu architecture but could not identify
the specific CSS causing hiding without live DOM inspection.
</context>

<next_action>
Get browser DevTools data from user (see remaining_work). Then:
- Wrong top/left → portal coordinate issue
- z-index conflict → add override targeting Ketcher root context
- display:none → find CSS rule hiding it
- Element missing from DOM → Ketcher JS issue
</next_action>
