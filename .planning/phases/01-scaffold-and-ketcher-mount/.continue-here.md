---
context: phase
phase: 01-scaffold-and-ketcher-mount
task: null
total_tasks: null
status: pre-planning
last_updated: 2026-05-18T12:58:56.277Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints identified this session — pre-planning artifacts completed without execution failures._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| SWC plugin crash | `@vitejs/plugin-react-swc` panics when processing Ketcher packages (issue #5565). Standard `@vitejs/plugin-react` (esbuild) is the correct choice. | blocking | CLAUDE.md and CONTEXT.md lock this — do not accept any plan or task that references the SWC variant |
| Conditional Ketcher render | Wrapping `<Editor>` in a conditional causes WASM re-initialization on remount, breaking state | blocking | D-09 in CONTEXT.md is locked; planner and executor must treat this as non-negotiable |
| WASM asset 404 in production | Without `vite-plugin-static-copy` + `assetsInlineLimit: 0`, WASM and worker files are missing at runtime | advisory | Phase 1 plan must include explicit asset-copy configuration tasks |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 1 pre-planning is complete. All prerequisite artifacts for `/gsd-plan-phase 1` are in place:
- `01-CONTEXT.md` — 15 locked design and technical decisions (D-01 through D-15)
- `01-RESEARCH.md` — confirmed WASM asset names, highlights API, CSS isolation findings, Google Fonts/COEP risk
- `01-VALIDATION.md` — Nyquist validation strategy (vitest-based, ~30s feedback cycle)
- `01-UI-SPEC.md` — approved UI design contract (6/6 dimensions passed; 2 non-blocking flags from verbatim design handoff values)

No PLAN.md files exist yet. Planning has not started.
</current_state>

<completed_work>

Completed this session:

- `01-CONTEXT.md` — created via `/gsd-discuss-phase 1`; 15 locked decisions captured (commit `913ca5b`)
- `01-RESEARCH.md` — phase researcher confirmed WASM asset list, `highlights.create` API location (`ketcher.editor.highlights`), no CSS token collisions, Google Fonts/COEP risk (commit `b9a9f33`)
- `01-VALIDATION.md` — Nyquist validation strategy written from RESEARCH.md template (commit `31ea196`)
- `01-UI-SPEC.md` — UI design contract created, required 2 revision cycles:
  - Round 1: moved future-phase typography sizes (19px, 14.5px) to HTML comment; added PLSH-03 override note for off-grid spacing values
  - Round 2: moved 18px/10px out of Spacing Scale table into "Verbatim Design Handoff Values (not spacing tokens)" section in CSS Architecture
  - Final result: 6/6 dimensions PASS, 2 FLAGs (non-blocking, both from verbatim design handoff) (commit `2915b14`)
</completed_work>

<remaining_work>

- **Create PLAN.md**: Run `/gsd-plan-phase 1` — the planner will find existing RESEARCH.md, VALIDATION.md, and UI-SPEC.md and go straight to planning (no research needed)
- **Execute Phase 1**: Run `/gsd-execute-phase 1` after plans are created and reviewed
</remaining_work>

<decisions_made>

Key decisions locked in CONTEXT.md (all D-XX entries):
- D-01/D-02: Vite 8 `react-ts` template; `@vitejs/plugin-react` (esbuild, NOT SWC)
- D-03/D-04: Ketcher packages pinned to 3.12.0; `StandaloneStructServiceProvider` at module level
- D-05: Full layout shell in Phase 1 (`<Header>`, `<KetcherPanel>`, stub containers)
- D-06: CSS Modules + CSS custom properties; no Tailwind
- D-07: Ketcher CSS wrapped in `@layer ketcher-reset { }` for cascade isolation
- D-08: Loading overlay — centered text over grey fill until `onInit` fires, then unmount
- D-09: `<Editor>` never conditionally rendered
- D-10/D-11: `vite-plugin-static-copy` copies WASM from `ketcher-standalone/dist/binaryWasm/`; `assetsInlineLimit: 0`
- D-12: Google Fonts via `<link>` with `crossorigin` attribute as COEP mitigation; fallback is self-hosting
- D-13: Netlify primary deployment target (not GitHub Pages); no `coi-serviceworker` needed
- D-14/D-15: No Zustand in Phase 1; `ketcher` ref in `useRef` on `App`

UI-SPEC decisions:
- Spacing scale: 4/8/16/24/32/48/64px standard tokens; 18px and 10px are verbatim handoff values (not tokens), exempt from grid rule under PLSH-03
- Typography: 4 normative sizes for Phase 1 (52px, 13px, 11px, 10.5px); 19px/14.5px deferred to Phase 3
</decisions_made>

<blockers>
None — all pre-planning artifacts complete, no blockers identified.
</blockers>

## Required Reading (in order)
1. `./CLAUDE.md` — project constraints, rejected choices, critical Ketcher API notes
2. `.planning/phases/01-scaffold-and-ketcher-mount/01-CONTEXT.md` — locked decisions D-01 through D-15; executor must not deviate from these
3. `.planning/phases/01-scaffold-and-ketcher-mount/01-RESEARCH.md` — WASM asset exact filenames, `highlights.create` API location, CSS isolation findings
4. `.planning/phases/01-scaffold-and-ketcher-mount/01-UI-SPEC.md` — approved design contract; planner uses this for visual/interaction specs

## Critical Anti-Patterns (do NOT repeat these)

- **SWC PLUGIN**: Any reference to `@vitejs/plugin-react-swc` must be rejected — it panics on Ketcher packages (issue #5565). Use `@vitejs/plugin-react` (standard esbuild variant) exclusively.
- **CONDITIONAL KETCHER RENDER**: `<Editor>` must never be inside a conditional — WASM re-initializes on remount. Always render it, use CSS to hide/show if needed.
- **highlights API location**: `highlights.create` is on `ketcher.editor.highlights`, NOT `ketcher.highlights` — the wrong path throws silently.

## Infrastructure State

- No dev server running
- No build artifacts
- Project directory: `/home/bsmue/code/InChI-vis`
- Node packages: not yet installed (Phase 1 execution will run `npm install`)

<context>
The session ran two commands: `/gsd-plan-phase 1` (which did research + validation but then correctly exited when the user chose to generate UI-SPEC first) and `/gsd-ui-phase 1` (which created and approved the UI design contract after 2 revision cycles). The project is a chemist-facing tool that renders an InChI string with interactive, color-coded layers linked to atom highlights in a Ketcher editor. Phase 1 is pure scaffolding — get the Vite + React + Ketcher stack running with the design shell in place. All the hard technical choices are locked in CONTEXT.md. The planner just needs to turn those decisions into executable tasks.
</context>

<next_action>
Start with: `/clear` then `/gsd-plan-phase 1`

All prerequisites exist: CONTEXT.md (15 locked decisions), RESEARCH.md (WASM/API/CSS findings), VALIDATION.md (vitest strategy), UI-SPEC.md (approved). The plan-phase workflow will detect these and skip research, going straight to spawning the planner.
</next_action>
