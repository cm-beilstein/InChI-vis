---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-06-08T09:54:28Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

<current_state>
Ad-hoc session. Two workstreams completed:
1. Added 20 drug preset molecules (committed: abe3f91).
2. Fixed Ketcher settings modal being clipped at top/bottom — removed overflow:hidden
   from module .ketcher and .canvasWrap, restored it on global .ketcher (styles.css),
   added canvas-wrap class to canvasWrap div, fixed canvas-meta formula bug
   (was showing "1S" instead of molecular formula).

4 files modified, NOT YET COMMITTED. 174/174 tests pass.
Prior /q and /p highlight fixes still need browser verification.
</current_state>

<completed_work>

- **commit abe3f91** — Added 20 drug molecules to preset picker (CIDs verified via PubChem API)
- **uncommitted** — Ketcher settings modal unclip:
  - Removed `overflow: hidden` from `.ketcher` and `.canvasWrap` in `KetcherPanel.module.css`
  - Added `overflow: visible` explicitly to `.canvasWrap` to override global `.canvas-wrap`
  - Added `border-radius: 5px 0 0 5px` to `.canvasWrap`, `0 5px 5px 0` to `.mol-list`
  - Restored `overflow: hidden` on global `.ketcher` in `styles.css` (clips toolbar overflow)
  - Added plain `canvas-wrap` class alongside module class on canvasWrap div in `KetcherPanel.tsx`
  - Fixed `App.tsx` line 131: `layers[0].text` → `layers.find(l => l.type === 'formula')?.text ?? null`
</completed_work>

<remaining_work>

- Browser-verify: settings modal no longer clipped at top/bottom
- Browser-verify: H/CNOS/PFClBrI element labels no longer visible outside editor bounds
- Browser-verify: canvas-meta shows correct formula (e.g. C₁₀H₁₄N₂ for nicotine, not "1S")
- Browser-verify prior /q /p fixes: thiamine /q+1, CuSO4 /q+2;, CuSO4 /p-2
- Commit the 4 modified files once all verification passes
</remaining_work>

<decisions_made>

- **overflow:hidden strategy**: Remove from module wrappers (modal escapes to .canvasWrap level),
  keep on global .ketcher (clips Ketcher's own toolbar overflow). The modal overlay uses
  position:absolute anchored to .canvasWrap (nearest position:relative), not to Ketcher's root.
- **canvas-wrap dual class**: CSS selector `.canvas-wrap .canvas-meta` in styles.css requires a
  plain `.canvas-wrap` ancestor. Module-scoped class alone never matched. Pre-existing bug:
  canvas-meta was in normal flow, hidden by overflow:hidden, and showing wrong data (layers[0]="1S").
- **formula fix**: `layers.find(l => l.type === 'formula')?.text` is correct. layers[0] is always
  the version layer (text: "1S"), not the formula.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `src/components/KetcherPanel.module.css` — current state of canvasWrap rule
2. `src/styles.css` lines 144–152 — global .ketcher rule (overflow:hidden restored here)
3. `src/App.tsx` line 131 — formula fix

## Infrastructure State
- Dev server: not running (start with `npm run dev`)
- Tests: 174/174 passing
- 4 files modified, unstaged: src/App.tsx, src/components/KetcherPanel.module.css,
  src/components/KetcherPanel.tsx, src/styles.css

<context>
Session opened with /gsd-resume-work. Prior /q /p fixes were already committed and pushed.
New work this session: 20 drug molecules added, then Ketcher settings modal CSS fix.
The modal fix uncovered a pre-existing canvas-meta bug (wrong formula, wrong positioning)
that was masked by overflow:hidden. Both the root cause (CSS selector mismatch) and the
data bug (layers[0] = version, not formula) were fixed in the same changeset.
</context>

<next_action>
Run `npm run dev`, open browser, verify:
1. Settings modal fully visible (not clipped)
2. No H/CNOS/PFClBrI text outside editor
3. Canvas-meta shows correct formula
4. /q+1 thiamine, /q+2; CuSO4, /p-2 CuSO4 highlights work
Then commit the 4 files.
</next_action>
