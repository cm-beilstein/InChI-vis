---
phase: "07"
plan: "02"
type: execute
wave: 1
depends_on:
  - "07-00"
files_modified:
  - src/lib/highlightUtils.ts
  - src/components/InchiSection.tsx
  - src/components/InchiSection.module.css
autonomous: true
requirements:
  - INCHI-07
  - PLSH-04

must_haves:
  truths:
    - "Hovering the p-layer highlights all heteroatom Ketcher IDs with --c-proton color"
    - "Hovering the p-layer on a pure-carbon molecule returns [] (no highlight, no error)"
    - "A copy icon button appears at the right end of the InChI display box when canvas is non-empty"
    - "Clicking the copy button writes the verbatim inchi string to the clipboard"
    - "After click, 'Copied!' confirmation text appears; it disappears after 1500ms"
    - "Copy button is hidden when canvas is empty"
  artifacts:
    - path: "src/lib/highlightUtils.ts"
      provides: "p-layer case in buildHighlightSpecs"
      contains: "case 'p'"
    - path: "src/components/InchiSection.tsx"
      provides: "copy button with state and handler"
      contains: "navigator.clipboard"
    - path: "src/components/InchiSection.module.css"
      provides: "copyBtn and copiedFeedback CSS classes"
      contains: ".copyBtn"
  key_links:
    - from: "src/lib/highlightUtils.ts"
      to: "src/styles.css"
      via: "--c-proton CSS token (already exists: oklch(0.55 0.14 325))"
      pattern: "--c-proton"
    - from: "src/components/InchiSection.tsx"
      to: "src/store.ts"
      via: "inchi read via useInchiStore(state => state.inchi) — verbatim string"
      pattern: "useInchiStore.*inchi"
---

<objective>
Implement p-layer protonation site highlighting and the copy-to-clipboard button.
These two features share no code and can be written in parallel or sequentially
within the same plan.

Purpose:
- INCHI-07: hovering the p-layer now highlights heteroatom atoms (N, O, S, P, etc.)
  instead of showing no highlight. This gives chemists a visual cue about which
  atoms are likely protonation sites.
- PLSH-04: a copy button lets users copy the verbatim InChI string with one click.

Output:
- highlightUtils.ts: 'p' removed from NON_SPATIAL; case 'p' added to switch
- InchiSection.tsx: copy button with useState feedback and clipboard handler
- InchiSection.module.css: copyBtn, copiedFeedback styles
- All RED tests from 07-00 for INCHI-07 and PLSH-04 turn GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-multi-fragment-highlighting-p-layer-and-copy/07-RESEARCH.md
@src/lib/highlightUtils.ts
@src/components/InchiSection.tsx
@src/components/InchiSection.module.css
@src/styles.css
@src/lib/__tests__/highlightUtils.test.ts
@src/__tests__/InchiSection.test.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/lib/highlightUtils.ts:
```typescript
// NON_SPATIAL guard at line 76 — contains 'p'; must remove 'p' from this array:
if (['version', 'q', 'p', 'i', 'b'].includes(layer.type)) { return []; }

// buildHighlightSpecs signature (unchanged):
export function buildHighlightSpecs(
  layer: Layer,
  subHover: SubHover | null,
  auxMap: AuxMap,
  atomElements: Record<number, string>,
  hAtomPoolIds: number[],
  layers: Layer[],
  struct: StructLike,
  resolveVarFn: (name: string) => string,
): HighlightSpec[]

// HighlightSpec shape:
export type HighlightSpec = {
  atoms: number[];
  bonds: number[];
  rgroupAttachmentPoints: number[];
  color: string;
};
```

From src/styles.css (--c-proton already exists at line 17):
```css
--c-proton:  oklch(0.55 0.14 325);
--c-proton-bg:  oklch(0.95 0.04 325);
```
No new CSS token needed.

From src/store.ts (InchiSection reads inchi via selector):
```typescript
const inchi = useInchiStore(state => state.inchi); // verbatim Ketcher output string
```

From src/components/InchiSection.module.css (existing empty-state rule):
```css
.inchiDisplay[data-empty="true"] { opacity: 0.45; ... pointer-events: none; }
```
The copy button must NOT appear inside the pointer-events:none empty-state box.
Hide it with a conditional in JSX (not CSS) using the isEmpty flag.

From src/hooks/useKetcherHighlights.ts (NON_SPATIAL guard also exists here):
```typescript
// Line 91: if (['version', 'q', 'p', 'i'].includes(layer.type)) { ... }
// Note: 'b' is NOT in this list — 'b' IS in highlightUtils NON_SPATIAL but NOT here.
// For p-layer: remove 'p' from this guard too, so buildHighlightSpecs is called.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add p-layer case to buildHighlightSpecs</name>
  <files>src/lib/highlightUtils.ts</files>
  <read_first>
    - src/lib/highlightUtils.ts — NON_SPATIAL list at line 76; switch statement; existing 'formula' case for pattern reference; buildHighlightSpecs signature
    - src/lib/__tests__/highlightUtils.test.ts — existing 'p layer: returns empty array []' test at line ~296 (this test will need to be updated to reflect new behavior); new INCHI-07 tests from 07-00
    - .planning/phases/07-multi-fragment-highlighting-p-layer-and-copy/07-RESEARCH.md — Finding 2, Finding 7, Pitfall 5, Code Examples section ('p-layer highlight in buildHighlightSpecs')
    - src/hooks/useKetcherHighlights.ts — NON_SPATIAL guard at line 91 also excludes 'p'
  </read_first>
  <behavior>
    buildHighlightSpecs case 'p':
    - atomElements: Record<number, string> maps canonical index → element symbol
    - Collect heteroatom canonical indices: filter entries where el !== 'C' && el !== 'H'
    - Map each canonical index through auxMap to get Ketcher IDs; filter undefined
    - If no heteroatom IDs: return [] (silent no-op — handles pure-carbon molecules)
    - Return one HighlightSpec: { atoms: heteroIds, bonds: [], rgroupAttachmentPoints: [], color: resolveVarFn('--c-proton') }

    NON_SPATIAL list change:
    - Remove 'p' from ['version', 'q', 'p', 'i', 'b'] → becomes ['version', 'q', 'i', 'b']

    useKetcherHighlights.ts companion change:
    - Line 91 guard: remove 'p' from ['version', 'q', 'p', 'i'] → becomes ['version', 'q', 'i']
    - This ensures the hook calls buildHighlightSpecs for p-layer hover events

    Existing test that must be UPDATED (not deleted):
    - The test 'p layer: returns empty array []' in highlightUtils.test.ts was written
      under the assumption p is non-spatial. It uses pLayer (text: '+1', atoms: []) with
      all-carbon atomElements {1:'C',2:'C',...}. After the fix this test should STILL pass
      because all-carbon atomElements produces no heteroatoms → returns []. Verify this:
      the existing test fixture has atomElements = {1:'C',2:'C',3:'C',4:'C',5:'C',6:'C'} and
      pLayer.atoms = []. Since no heteroatoms exist, the new case 'p' returns [] — the test
      continues to pass without modification. No test update needed.
  </behavior>
  <action>
    In highlightUtils.ts line 76, change the NON_SPATIAL check from:
      ['version', 'q', 'p', 'i', 'b'].includes(layer.type)
    to:
      ['version', 'q', 'i', 'b'].includes(layer.type)

    Add a new case 'p': block inside the switch statement, immediately before the
    case 'm': block. The block iterates Object.entries(atomElements), filters for
    el !== 'C' && el !== 'H', maps canonical indices through auxMap, filters undefined,
    returns [] if empty, otherwise returns one HighlightSpec with color resolveVarFn('--c-proton').

    In useKetcherHighlights.ts line 91, change the early-return guard from:
      ['version', 'q', 'p', 'i'].includes(layer.type)
    to:
      ['version', 'q', 'i'].includes(layer.type)

    Do not change buildSubHoverSpecs — p-layer has no sub-tokens.
  </action>
  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npm test -- --run src/lib/__tests__/highlightUtils.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>
    All tests in highlightUtils.test.ts pass, including new INCHI-07 p-layer tests.
    buildHighlightSpecs with atomElements containing N returns specs[0].color === '--c-proton'.
    buildHighlightSpecs with all-C atomElements returns [].
    The existing 'p layer: returns empty array []' test passes (pure-carbon no-op preserved).
    npm test exits 0 for highlightUtils test file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add copy-to-clipboard button to InchiSection</name>
  <files>
    src/components/InchiSection.tsx
    src/components/InchiSection.module.css
  </files>
  <read_first>
    - src/components/InchiSection.tsx — full file; isEmpty logic; inchiDisplay div structure; existing store selectors
    - src/components/InchiSection.module.css — existing classes; .inchiDisplay structure; .emptyHint
    - src/__tests__/InchiSection.test.tsx — new PLSH-04 copy button tests from 07-00; existing store mock shape (check mockInchi variable added in 07-00)
    - .planning/phases/07-multi-fragment-highlighting-p-layer-and-copy/07-RESEARCH.md — Finding 6, Code Examples section ('Copy button in InchiSection.tsx'), Pitfall 4, Pitfall 6
    - memory/feedback_inchi_passthrough.md (via context system) — NEVER reconstruct InChI; use verbatim store value
  </read_first>
  <behavior>
    InchiSection.tsx changes:
    - Add import { useState } from 'react' (check if already imported)
    - Add const [copied, setCopied] = useState(false) inside the component
    - Add handleCopy async function: await navigator.clipboard.writeText(inchi); setCopied(true); setTimeout(() => setCopied(false), 1500)
    - handleCopy must use try/catch — silent failure (no error shown) per Finding 6
    - Copy button: render only when !isEmpty (conditional JSX, not CSS display:none)
    - Button placement: at the end of the non-empty branch JSX, after the layers.map fragment
      and BEFORE the closing </> of the non-empty branch. Position it as an absolutely-positioned
      element inside inchiDisplay using CSS, OR append it as a sibling after the layer spans
      with CSS float/flex alignment
    - Preferred placement: inside the inchiDisplay div, after the layer spans, styled with
      styles.copyBtn class that positions it at the right edge
    - Copy icon: inline SVG clipboard icon (two-rectangle clipboard shape, 16x16 viewBox).
      Use a simple path-based SVG — no external icon library. The SVG must use currentColor
      for stroke/fill so it inherits color from the button's CSS color property
    - Copied feedback: a <span className={styles.copiedFeedback}> showing 'Copied!' rendered
      when copied === true, otherwise null
    - Button aria-label: 'Copy InChI to clipboard'

    InchiSection.module.css new classes:
    - .copyBtn: position absolute right 10px top 50% transform translateY(-50%); background none;
      border none; cursor pointer; color var(--ink-faint); padding 4px; border-radius 3px;
      transition color 160ms; line-height 0 (for SVG sizing)
    - .copyBtn:hover: color var(--c-formula)
    - .copyBtn svg: width 16px height 16px display block
    - .copiedFeedback: position absolute right 36px top 50% transform translateY(-50%);
      font-family var(--font-mono); font-size 11px; color var(--c-formula); white-space nowrap;
      pointer-events none

    Verbatim inchi requirement (from memory):
    - Use the inchi string from store directly: useInchiStore(state => state.inchi)
    - Never join layer.text fields. This is a hard project invariant.

    .inchiDisplay already has padding 8px 14px. Ensure right-side padding is sufficient
    to not overlap copy button: add padding-right 40px to .inchiDisplay in the module CSS
    (or use a wrapper approach). Choose the approach that does not require changes to
    the existing padding shorthand — add padding-right: 40px as a separate declaration
    after the existing padding rule.
  </behavior>
  <action>
    In InchiSection.tsx:
    1. Add useState to the React import
    2. Add const [copied, setCopied] = useState(false) after the existing store selectors
    3. Add async function handleCopy() with try/catch, clipboard write, and timeout
    4. In the non-empty JSX branch, after the layers.map(...) expression and before </>,
       add the copy button and feedback span:
       - button element with className={styles.copyBtn}, onClick={handleCopy}, aria-label='Copy InChI to clipboard'
       - Inside the button: inline SVG 16x16 clipboard icon
       - After the button (as sibling inside inchiDisplay): {copied && <span className={styles.copiedFeedback}>Copied!</span>}
    5. Add position relative to .inchiDisplay in InchiSection.module.css (required for
       absolute-positioned children)

    In InchiSection.module.css:
    1. Add position: relative to the existing .inchiDisplay rule
    2. Add .copyBtn rule with absolute positioning, right:10px, top:50%, transform, no border/bg,
       cursor:pointer, color:var(--ink-faint), transition, line-height:0
    3. Add .copyBtn:hover with color:var(--c-formula)
    4. Add .copyBtn svg with width:16px, height:16px, display:block
    5. Add .copiedFeedback with absolute position, right:36px, top:50%, transform, font-mono,
       font-size:11px, color:var(--c-formula), white-space:nowrap, pointer-events:none
  </action>
  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npm test -- --run src/__tests__/InchiSection.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>
    All tests in InchiSection.test.tsx pass, including new PLSH-04 copy button tests.
    Copy button renders when layers non-empty; absent when empty.
    navigator.clipboard.writeText called with verbatim inchi string on click.
    'Copied!' text appears after click; disappears after fake timer advance to 1500ms.
    Existing PLSH-01 empty-state tests still pass.
    npm test exits 0 for InchiSection test file.
    TypeScript: npm run build (or npx tsc --noEmit) completes without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → Clipboard API | navigator.clipboard.writeText receives the verbatim Ketcher InChI string; no user-provided HTML or script content is written |
| hover → Ketcher highlights | p-layer produces Ketcher atom IDs from atomElements; IDs are numeric, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | Clipboard API | accept | InChI string is public chemistry data; no PII or secrets involved |
| T-07-04 | Tampering | p-layer highlight | accept | atomElements comes from formula layer parsing (trusted local computation); not user input |
| T-07-05 | Denial of Service | setTimeout leak | mitigate | Use clearTimeout in handleCopy before setting a new timer; or accept minor leak (1.5s max, low risk for a single-page tool); recommend clearing in production |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
Run full test suite:

```bash
cd /home/bsmue/code/explain-that-inchi && npm test -- --run 2>&1 | tail -20
```

All tests pass. Confirm TypeScript compiles:

```bash
cd /home/bsmue/code/explain-that-inchi && npx tsc --noEmit 2>&1 | head -20
```

No errors.
</verification>

<success_criteria>
- buildHighlightSpecs('p' layer with N atom, ...) returns spec with color '--c-proton' and heteroatom Ketcher IDs
- buildHighlightSpecs('p' layer with all-C atoms, ...) returns []
- Copy button renders in the inchiDisplay div when !isEmpty
- Copy button is absent from DOM when isEmpty
- Clicking copy button calls navigator.clipboard.writeText with the verbatim inchi string
- 'Copied!' text appears after click, disappears after 1500ms
- npm test -- --run exits 0 (all tests pass, no regressions)
- npx tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/07-multi-fragment-highlighting-p-layer-and-copy/07-02-SUMMARY.md` when done
</output>
