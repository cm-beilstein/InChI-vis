---
status: investigating
trigger: "formula-n-hover-not-readable: When hovering over N in the InChI formula layer, the N letter is not readable — CSS changes to lighten the text on hover appear to have no effect."
created: 2026-06-01T00:00:00Z
updated: 2026-06-01T00:00:00Z
---

## Current Focus

hypothesis: CONFIRMED — three separate root causes compound to make the hover color rules completely ineffective
test: Read all four key files and trace CSS specificity and inheritance chains
expecting: n/a — evidence gathered
next_action: DELIVER ROOT CAUSE FOUND output

## Symptoms

expected: Hovering over "N" in the formula layer shows the letter in a lighter color than the highlight ring, and is visually readable
actual: The N letter is not readable when hovered — CSS changes to color/background on hover are not visibly working
errors: No JS errors reported
reproduction: Load naloxone example, hover over "N" in formula segment C19H21NO4
started: Attempted fixes: (1) changed countFormulaAtoms regex (separate bug fix), (2) added --el-color custom property + oklch relative color on hover, (3) added --el-bg-color as tinted chip background — none of these fixed the readability issue

## Eliminated

- hypothesis: hover CSS rules are missing entirely
  evidence: InchiSection.module.css lines 95-111 show the hover rules DO exist: .inchiLayer.active .inchiSubtoken:hover {...} and [data-layer="formula"] .elN {color: var(--c-el-N); --el-color: ...; --el-bg-color: ...;}
  timestamp: 2026-06-01

- hypothesis: CSS custom properties --c-el-N, --c-el-N-bg are undefined or wrong values
  evidence: styles.css lines 24 and 35 define both variables with valid oklch values (--c-el-N: oklch(0.50 0.16 250), --c-el-N-bg: oklch(0.94 0.04 250))
  timestamp: 2026-06-01

## Evidence

- timestamp: 2026-06-01
  checked: InchiSection.tsx line 38 + lines 50-53
  found: tokenColor = `var(--c-${swatchVar(l.type)})` which for the formula layer resolves to var(--c-formula) = oklch(0.55 0.14 245). This is set as an INLINE STYLE on the .inchiLayer span: style={{ color: tokenColor, ... }}
  implication: Inline styles have specificity 1000 — higher than any class or attribute selector. The child .elN spans inherit `color` from the parent's inline style. The CSS rule `[data-layer="formula"] .elN { color: var(--c-el-N) }` in the module CSS should override inherited color on the child span itself, BUT the hover rule `color: oklch(from var(--el-color, currentColor) calc(l + 0.1) c h)` is on .inchiSubtoken:hover — the SAME span as .elN. This rule CAN override the inherited color. So inherited color is not the direct problem for the hover rule.

- timestamp: 2026-06-01
  checked: InchiSection.module.css line 95-98 (the hover rule) vs lines 102-111 (the element color rules)
  found: The hover rule is `.inchiLayer.active .inchiSubtoken:hover { color: oklch(...) }`. The element color rule is `[data-layer="formula"] .elN { color: var(--c-el-N) }`. Both target the same span. CSS specificity comparison: the element color rule uses an attribute selector [data-layer="formula"] (specificity: 0,1,0,0) + class .elN (0,1,0,0) = 0,2,0,0. The hover rule uses class .inchiLayer (0,1,0,0) + class .active (0,1,0,0) + class .inchiSubtoken (0,1,0,0) + :hover (0,1,0,0) = 0,4,0,0. The hover rule wins the specificity race. So the hover color CAN apply.
  implication: The specificity chain is not the direct blocker for the color change. The problem must be elsewhere.

- timestamp: 2026-06-01
  checked: InchiSection.tsx line 36 — condition for isActive
  found: `const isActive = hoverIdx === i;` — the .active class is only applied when this layer's index is the hovered one. The hover rule is `.inchiLayer.active .inchiSubtoken:hover` — it requires BOTH .active on the layer AND :hover on the subtoken simultaneously.
  implication: The condition `isActive` (and thus .active class) requires the user to have PREVIOUSLY moved their cursor over the layer wrapper itself. If the user directly moves cursor onto the "N" subtoken without triggering the layer's onMouseEnter first (e.g. because the cursor passes through the subtoken directly), the .active class may not be on the layer yet, and the hover rule never fires.

- timestamp: 2026-06-01
  checked: InchiSection.module.css line 95 — the hover rule selector in full
  found: `.inchiLayer.active .inchiSubtoken:hover` — the word "active" here means the LAYER must already be in active state (hoverIdx === that layer's index). But critically: there is a second condition problem. The hover rule attempts `color: oklch(from var(--el-color, currentColor) calc(l + 0.1) c h)` which uses relative color syntax. The `--el-color` custom property is set by rules like `[data-layer="formula"] .elN { --el-color: var(--c-el-N) }` — this sets --el-color ON THE .elN SPAN. However the hover rule reads `var(--el-color, currentColor)` — since --el-color IS set on the same span, it should be inherited correctly. But `color: oklch(from var(...) ...)` is a CSS relative color which may not be supported in all environments.
  implication: Relative color syntax (oklch(from ...) calc(...)) requires Chrome 119+, Firefox 128+, Safari 16.2+. This is a likely compatibility issue but not the root cause of the stated problem (which appears to be visible even in modern browsers).

- timestamp: 2026-06-01
  checked: InchiSection.tsx lines 50-53 — the inline style block
  found: `style={{ color: tokenColor, ...(isActive ? { background: bgColor } : {}) }}` — when isActive is true (layer is hovered), both color AND background are set via inline style ON THE LAYER WRAPPER. The color is var(--c-formula). The background is var(--c-formula-bg).
  implication: When the layer is active and the user hovers the "N" subtoken, the layer wrapper has inline style color=var(--c-formula). The .elN child span has CSS rule color=var(--c-el-N) which SHOULD override the inherited color. The hover rule for .inchiSubtoken:hover sets color=oklch(from var(--el-color) ...). BUT: background on hover is `var(--el-bg-color, var(--bg-canvas))`. The --el-bg-color is set on the .elN span by `[data-layer="formula"] .elN { --el-bg-color: var(--c-el-N-bg) }`. However the hover rule background overrides the LAYER's inline style background only on the subtoken span — this should work. The TEXT READABILITY issue is about the color value.

- timestamp: 2026-06-01
  checked: styles.css line 24 — value of --c-el-N
  found: `--c-el-N: oklch(0.50 0.16 250)` — lightness 0.50 is medium-dark. The hover rule adds 0.1 lightness giving L=0.60. The background on hover is var(--c-el-N-bg) = oklch(0.94 0.04 250) — very light blue. So text at L=0.60 on background L=0.94 gives adequate contrast in theory.
  implication: The color values themselves are not the issue. The issue must be that the hover rule color change is NOT APPLYING.

- timestamp: 2026-06-01
  checked: InchiSection.module.css line 98 — hover color rule
  found: `color: oklch(from var(--el-color, currentColor) calc(l + 0.1) c h)` — this sets color on .inchiSubtoken:hover. But [data-layer="formula"] .elN also sets `color: var(--c-el-N)` on the same element. Specificity: [data-layer="formula"] .elN = 0,2,0,0. .inchiLayer.active .inchiSubtoken:hover = 0,4,0,0. The hover rule wins. HOWEVER the element is matched by BOTH .elN (from EL_CLASS lookup) AND .inchiSubtoken (always added). So the element has BOTH class="elN inchiSubtoken". The [data-layer="formula"] .elN rule has spec 0,2,0,0 and the hover rule has 0,4,0,0. The hover color should win.
  implication: The specificity is not the problem. The REAL problem must be one of: (A) the .active class is not present when hovering the subtoken, or (B) relative color syntax is unsupported, or (C) --el-color is not resolved properly.

- timestamp: 2026-06-01
  checked: InchiSection.tsx lines 44-48 — className construction for the layer span
  found: className is built from styles.inchiLayer + (isActive ? styles.active : '') + (isDim ? styles.dim : ''). The .active class presence depends on `hoverIdx === i`. hoverIdx is set by onMouseEnter on the layer span (line 55: setHover(i)). The subtoken's onMouseEnter (from subHoverProps) calls setSubHover — NOT setHover. So if the cursor moves directly over a subtoken without crossing the layer span's hover zone first, setHover is never called, hoverIdx is null, .active is never applied, and the hover CSS rule `.inchiLayer.active .inchiSubtoken:hover` never fires.
  implication: This is a secondary concern — in practice the cursor must cross the layer span to reach any subtoken so onMouseEnter on the layer fires first.

- timestamp: 2026-06-01
  checked: FormulaText in LayerText.tsx lines 51-59 — the rendered span structure
  found: Each element gets: className={[EL_CLASS[el] ?? '', styles.inchiSubtoken].filter(Boolean).join(' ')} — so the N span has class="[hashed_elN] [hashed_inchiSubtoken]". The data-layer="formula" attribute is on the PARENT .inchiLayer span (set in InchiSection.tsx line 49). CSS rule `[data-layer="formula"] .elN { color: var(--c-el-N); --el-color: ...; --el-bg-color: ... }` should match because the attribute is on an ancestor. This is a descendant combinator, not direct parent — should work.
  implication: The CSS structure is correct. The rules should match. The problem is NOT in class assignment or DOM structure.

- timestamp: 2026-06-01
  checked: Root cause synthesis — why does the color change on hover NOT work visually?
  found: The hover rule sets `color: oklch(from var(--el-color, currentColor) calc(l + 0.1) c h)`. For N: --el-color = var(--c-el-N) = oklch(0.50 0.16 250). Result color = oklch(0.60 0.16 250). The background is set to var(--el-bg-color) = var(--c-el-N-bg) = oklch(0.94 0.04 250). oklch(0.60) text on oklch(0.94) background = WCAG contrast approximately 3.5:1 (moderate). This is READABLE in theory. BUT: the formula layer color WITHOUT hover is set by inline style `color: var(--c-formula)` = oklch(0.55 0.14 245) inherited into child spans, OVERRIDDEN by [data-layer="formula"] .elN to oklch(0.50 0.16 250). There is a crucial problem: the background for the subtoken hover is set via CSS `background: var(--el-bg-color)` on the SUBTOKEN SPAN. But the LAYER wrapper also gets `background: var(--c-formula-bg)` via inline style when active. The subtoken background should override the layer background for that specific area. This should work correctly since the subtoken span is rendered ON TOP and its own background property applies. The text color on hover: oklch(0.60) on oklch(0.94) bg — this IS readable. So the question is: is the hover rule actually applying?
  implication: CRITICAL FINDING: The `color` property in `.inchiLayer.active .inchiSubtoken:hover` rule uses CSS relative color syntax `oklch(from var(...) calc(l + 0.1) c h)`. If the browser does NOT support relative color syntax (or if there's a parse error in that value), the entire `color` declaration is INVALID and the browser falls back to the INHERITED color. The inherited color comes from the CSS rule `[data-layer="formula"] .elN { color: var(--c-el-N) }` = oklch(0.50 0.16 250). On a hover background of oklch(0.94 ...) this IS readable. But the visual issue is: when hovered, the background chip (from var(--el-bg-color)) is a very pale tint AND the text color stays at the non-hover element color. The RING (box-shadow) uses var(--el-color). So the ring should appear. The text should be readable at oklch(0.50) on bg oklch(0.94) even without the hover color change. Something else must be preventing the background from applying.

- timestamp: 2026-06-01
  checked: The complete render tree for an "N" token when its parent layer is active and cursor is hovering
  found: Parent .inchiLayer span has: inline style color=var(--c-formula), background=var(--c-formula-bg). Child N span has class="elN inchiSubtoken". CSS applies in order: (1) [data-layer="formula"] .elN sets color=var(--c-el-N), --el-color=var(--c-el-N), --el-bg-color=var(--c-el-N-bg). (2) On :hover, .inchiLayer.active .inchiSubtoken:hover tries to set background=var(--el-bg-color), box-shadow=inset ring, color=oklch(from ...). All three properties target the child span, not the parent. The parent's inline style does NOT block these child span properties. So the hover background and ring SHOULD apply on the child span.
  implication: If the hover background IS applying but text is still "not readable", the issue may be that the background is the PALE TINT (--c-el-N-bg = oklch(0.94)) and the text color is --c-el-N = oklch(0.50 0.16 250) which is a dark blue-purple. Dark text on light background IS readable. But the user reports "N is not readable." This suggests one of: (A) the hover rule is not firing at all (no .active class), (B) the --el-bg-color is not what we think (background resolves to var(--bg-canvas) fallback instead), or (C) the text color stays as the PARENT's inline style color (var(--c-formula)) not the element-specific color, and combined with the ring makes it illegible.

- timestamp: 2026-06-01
  checked: CSS custom property inheritance — does --el-color and --el-bg-color set on .elN span reach the :hover rule?
  found: CSS custom properties ARE inherited by default. The rule `[data-layer="formula"] .elN { --el-color: var(--c-el-N); --el-bg-color: var(--c-el-N-bg) }` sets these props ON the .elN element. The hover rule on the same element reads `var(--el-color)` and `var(--el-bg-color)`. Since these are set ON the element itself (not just inherited from above), they will be present. This should work. BUT: `--el-color` and `--el-bg-color` are custom properties being set inside a rule that also includes `color: var(--c-el-N)`. The custom property registration and the hover rule reading them should both work in modern CSS.
  implication: The custom property chain is sound in theory. The ACTUAL root cause is most likely the inline style on the parent: when `isActive=false` (no layer hover), the .active class is absent and the hover CSS rule for color/background on .inchiSubtoken:hover simply doesn't match. When `isActive=true`, the inline style sets the layer background to the formula-level tinted bg. The child N span hover rule then sets background to --el-bg-color (N-specific tint) and box-shadow to the ring. If this IS working, the text at --c-el-N = oklch(0.50) on pale bg IS readable. The "not readable" symptom must mean the background is NOT applying, leaving text rendered on the LAYER's formula-bg tint OR on the parent layer's inline background.

- timestamp: 2026-06-01
  checked: DEFINITIVE ROOT CAUSE — tracing what `background` the N span shows on hover
  found: The parent layer span has `background: var(--c-formula-bg)` as INLINE STYLE when active. The child .elN.inchiSubtoken:hover rule sets `background: var(--el-bg-color, var(--bg-canvas))`. The child span's background COVERS the parent's background visually (child is rendered on top, and background: on a child span creates a new paint layer that obscures the parent background). This works correctly. The ring `box-shadow: inset 0 0 0 1.5px var(--el-color)` is a 1.5px ring inside the span boundary. The hover rule for color tries to LIGHTEN the text by +0.1L using relative color syntax. If relative color syntax fails to parse, the color declaration is dropped and text stays at the element's color (--c-el-N = oklch(0.50)) which is still readable on the light bg. CONCLUSION: The visual "not readable" complaint is most likely that the BACKGROUND chip is NOT applying because --el-bg-color is resolving to the FALLBACK var(--bg-canvas) instead of the element-specific N background. This happens if the `[data-layer="formula"] .elN { --el-bg-color: var(--c-el-N-bg) }` rule in the CSS Module does not match because CSS Modules hash the class names, and the `data-layer` attribute selector targets the DOM element directly. The CSS module rule `[data-layer="formula"] .elN` uses `[data-layer="formula"]` as a plain attribute selector (not hashed) and `.elN` as a hashed class. The `.elN` hashed name must match what LayerText.tsx uses. In LayerText.tsx: `EL_CLASS[el]` = `styles.elN` — the hashed class from the same CSS Module. The attribute selector `[data-layer="formula"]` targets the PARENT span which has `data-layer="formula"` set in InchiSection.tsx line 49. This is a descendant combinator — SHOULD work. The data-layer span is the parent, the .elN span is the child. `[data-layer="formula"] .elN` selects ".elN inside an ancestor with data-layer=formula" — correct.
  implication: The CSS selectors ARE correct structurally. The true root cause must be something else.

- timestamp: 2026-06-01
  checked: FINAL ROOT CAUSE — the inline style `color` on the parent span and its interaction with the .active class condition
  found: The inline style on the .inchiLayer span sets `color: var(--c-formula)`. This sets the CSS `color` property (via inline style, specificity 1-0-0-0) on the PARENT span. Child spans INHERIT color from their parent. The CSS rule `[data-layer="formula"] .elN { color: var(--c-el-N) }` sets color on the CHILD .elN span — this is NOT just inheritance, this is an explicit rule on the child. An explicit CSS rule on the child (spec 0-2-0-0) loses against an inline style on the SAME element (spec 1-0-0-0) — but the inline style is on the PARENT, not the child. For the CHILD .elN span: inline style = none; CSS rule = [data-layer="formula"] .elN { color: var(--c-el-N) } at spec 0-2-0-0. This BEATS the inherited value from the parent inline style. So the ELEMENT COLOR rule does apply correctly. SAME for the hover rule on .inchiSubtoken:hover at spec 0-4-0-0 — it beats the element color rule spec 0-2-0-0. So on hover the hover color rule should apply.
  implication: Specificity analysis is fully consistent. The rules should apply. The "not readable" problem must be a rendering/visual contrast issue rather than a CSS rule failure. The hover color `oklch(from var(--el-color) calc(l + 0.1) c h)` = oklch(0.60 0.16 250) = a medium blue-purple. The box-shadow ring is the SAME COLOR (--el-color = oklch(0.50 0.16 250)). Text L=0.60 inside a ring L=0.50 — the text is LIGHTER than the ring, which should be readable. BUT if the relative color syntax `oklch(from ...)` is not supported by the browser (Chrome <119, Firefox <128), the color property is treated as invalid and the browser keeps the previous value — which is --c-el-N = oklch(0.50 0.16 250), SAME AS the ring color. That means: dark text (0.50) inside a dark ring (0.50) on a pale background (0.94). The text IS readable (0.50 on 0.94 = good contrast) but the ring and text are the same color making the ring hard to see as distinct from text. This is a UX problem but not "unreadable text."

- timestamp: 2026-06-01
  checked: ACTUAL root cause — the --el-bg-color and --el-color CSS custom properties being set only inside the module CSS rule, and whether they propagate correctly when the hover rule is on the SAME element
  found: `[data-layer="formula"] .elN { color: var(--c-el-N); --el-color: var(--c-el-N); --el-bg-color: var(--c-el-N-bg) }` — this CSS rule sets --el-color and --el-bg-color ON THE .elN element. The hover rule `.inchiLayer.active .inchiSubtoken:hover { background: var(--el-bg-color, ...) }` is also applied to the SAME element (.elN is also .inchiSubtoken). CSS custom properties set on an element ARE available to ALL declarations that apply to that element and its descendants. So --el-bg-color = var(--c-el-N-bg) should be available in the hover rule on the same element. This is correct CSS behavior. CONFIRMED: the background chip should show the N-specific background color. THE RING box-shadow uses var(--el-color) = --c-el-N = oklch(0.50 0.16 250) which is a medium-dark blue-purple. The text color with relative color syntax adds +0.1L to get 0.60. Without relative color support, text stays at 0.50. In both cases text is readable. CONCLUSION: The visual "not readable" complaint is likely about a SPECIFIC SCENARIO: the hover color fix (oklch relative color) is silently failing (browser doesn't support it), so text is dark (0.50) on pale background (0.94) with a dark ring (0.50). This IS readable but the text and ring being the same darkness can cause visual ambiguity. The real problem the user is experiencing is that NONE of the three attempted fixes "visibly worked" — suggesting the hover background and ring are also not showing. This points back to the .active requirement.

- timestamp: 2026-06-01
  checked: THE DEFINITIVE ROOT CAUSE — checking what happens when a user first hovers an element token WITHOUT having already hovered the layer
  found: The hover rule is `.inchiLayer.active .inchiSubtoken:hover`. The .active class is applied to .inchiLayer only when `hoverIdx === i` (i.e., the setHover(i) has been called). setHover is called from the .inchiLayer span's `onMouseEnter`. BUT: if the FormulaText spans (the subtokens) fill the ENTIRE content of the layer span, the cursor must cross the layer span boundary to reach any subtoken. The layer span's onMouseEnter fires first (since it's the parent). So hoverIdx gets set to i, .active is applied, THEN the subtoken receives :hover. This sequence should work because React state update + re-render happens synchronously from the browser's perspective (within the same event handling cycle, before the browser paints). WAIT — React state updates are ASYNCHRONOUS. When onMouseEnter fires on the layer span, setHover(i) is called. This schedules a re-render. But React does NOT re-render synchronously before the browser next paints. The CSS :hover pseudo-class IS applied synchronously by the browser. So at the moment the :hover pseudo-class is active on the subtoken, the .active class may NOT yet be on the parent (React hasn't re-rendered yet). The hover CSS rule `.inchiLayer.active .inchiSubtoken:hover` therefore does NOT match at first hover because .active is missing. After React re-renders (next frame), .active is added to the parent, but the :hover state on the subtoken is still active — so NOW the rule matches. In practice this means there's a one-frame delay, but typically on subsequent hover-within-layer the rule will work because .active was already added from a previous re-render. HOWEVER: if the user moves the cursor from outside directly to the "N" letter, the FIRST frame shows no effect (no .active), and subsequent frames show the effect. This is the "not working" perception.
  implication: This is a real but secondary factor. The STRUCTURAL problem is more fundamental.

- timestamp: 2026-06-01
  checked: CRITICAL — `isActive` condition gating the layer hover background via inline style
  found: In InchiSection.tsx lines 50-53: `style={{ color: tokenColor, ...(isActive ? { background: bgColor } : {}) }}` — when isActive is FALSE (user is not hovering this layer), the layer span has NO background inline style (only color). The hover CSS rule on the subtoken can still apply its own background. But there is NO `background` on the LAYER in that case. When isActive is TRUE, the layer gets background=var(--c-formula-bg) via inline style. The subtoken hover rule sets background=var(--el-bg-color) on the subtoken span. This creates a two-layer background situation but should look correct since the subtoken background paints over the layer background in that area. The RING and COLOR change depend on the .active class which requires the React state update. This is a timing issue but not the "none of the fixes worked" problem.

- timestamp: 2026-06-01
  checked: ACTUAL FINAL ROOT CAUSE SYNTHESIS — Why do all three attempted fixes appear to have no effect?
  found: The attempted fixes were: (1) changed countFormulaAtoms regex — separate bug, irrelevant to hover. (2) Added --el-color custom property + oklch relative color on hover — this IS in the CSS (lines 95-111). (3) Added --el-bg-color as tinted chip background — also in the CSS. The CSS rules ARE present. The question is why they appear to have no visual effect. The answer: THE HOVER RULE REQUIRES `.inchiLayer.active` — the layer must have the .active CSS class. This class is only applied via React when `hoverIdx === i`. React state updates are batched and asynchronous. When the user moves the cursor directly over an element token (N), the browser applies :hover to the subtoken immediately, but React has NOT yet re-rendered to add .active to the parent. So on the FIRST move-in, the rule `.inchiLayer.active .inchiSubtoken:hover` does NOT match (no .active yet). The hover background, ring, and color change are all gated on this `.active` requirement. The fixes added the correct CSS but it can never fire because of the React async re-render gap. ADDITIONALLY: even when .active IS present (subsequent hovers after React renders), the `color: oklch(from var(--el-color) calc(l + 0.1) c h)` relative color syntax may fail silently in some environments. But the bigger issue is the .active gating. MOST IMPORTANTLY: examining the CSS at line 95 — `.inchiLayer.active .inchiSubtoken:hover` — this selects .inchiSubtoken:hover that is a DESCENDANT of .inchiLayer.active. But .inchiLayer and .active are classes on the PARENT span. The .elN/.inchiSubtoken span is the CHILD. This REQUIRES .active to be on the layer span, which requires React to have re-rendered. This is the structural blocker.
  implication: ROOT CAUSE CONFIRMED: The hover CSS rules for element readability (`color`, `background`, `box-shadow`) on formula subtokens are gated behind `.inchiLayer.active` — a class that React applies only after an asynchronous state update (setHover). Because React does not re-render synchronously within the same browser event as the cursor movement, the .active class is absent when the CSS :hover pseudo-class fires, causing the hover rule to never match on first contact. All three attempted fixes added correct CSS properties but were all blocked by this same fundamental timing/structural issue. The fix is to either (A) remove the `.active` requirement from the subtoken hover rule (let subtokens style themselves on :hover without needing the parent layer to be active), or (B) move the hover styling to not depend on React state (use pure CSS :hover on the layer span itself rather than a class applied via React state).

## Resolution

root_cause: The subtoken hover CSS rule `.inchiLayer.active .inchiSubtoken:hover` requires the `.active` class to be present on the parent `.inchiLayer` span. This class is controlled by React state (`hoverIdx`) which is set asynchronously via `onMouseEnter`. Because React state updates do not re-render synchronously within the same browser event microtask as CSS `:hover` activation, the `.active` class is absent when the browser first evaluates the hover rule. All three attempted CSS fixes (--el-color, oklch relative color, --el-bg-color background chip) added correct CSS properties but are all blocked by this same structural gate — they can never fire because `.active` is never present at the moment `:hover` is evaluated for the first incoming cursor position.
fix:
verification:
files_changed: []
