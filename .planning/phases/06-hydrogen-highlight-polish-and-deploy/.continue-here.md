---
context: phase
phase: 06-hydrogen-highlight-polish-and-deploy
task: 3
total_tasks: 8
status: in_progress
last_updated: 2026-05-22T12:46:10.982Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: OUT-OF-SCOPE EXECUTOR — The 06-00 executor agent staged 30+ files (phase-05 deletions, source file modifications, planning file deletions) that were NOT in its `files_modified` list. This required a full `git reset HEAD && git checkout -- .` to undo. Structural mitigation: before an executor commits anything, verify every staged file is in the plan's `files_modified` list. If files outside that list are staged, unstage them before committing.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Executor over-staging | The 06-00 agent ran `git add` on 30+ files including phase-05 components, planning files, and source files unrelated to test stubs. The plan's `files_modified` only had 2 test files. | blocking | Before any commit, run `git diff --cached --name-only` and verify every file is in the plan's `files_modified` list. Unstage anything not listed. |
| Agent Bash permission denial | Both 06-00 and 06-01 agents stalled because Bash was denied when they tried to run tests and git commit. They returned "completed" but left work unfinished. | advisory | When spawning executor agents, ensure Bash permission is granted. Check SUMMARY.md and git log after agent completes to verify actual completion. |

<current_state>
Phase 06 execution is mid-flight. Wave 1 (06-00, 06-01, 06-03) was spawned but is partially complete. Worktrees are still alive. The main branch (master) is at 59d66d8.

**Master branch state:**
- Committed (59d66d8): `src/__tests__/InchiSection.test.tsx` (06-00 Task 1), `src/__tests__/setup.ts`, `vitest.config.ts`, `package.json` (added @testing-library/react, @testing-library/jest-dom, happy-dom)
- Uncommitted in working tree: `src/lib/__tests__/highlightUtils.test.ts` has +62 lines (H-branch test stubs = 06-00 Task 2), plus many UI component changes (InchiSection.tsx, Header.tsx, KetcherPanel.tsx, styles.css, etc. — these are design-handoff changes from prior work, NOT part of phase 06)

**Active worktrees:**
- `agent-a0e5e1e3c6eb28f7b` (06-00) — at e9f6151 (old base, pre-59d66d8). Worktree was reset after agent staged wrong files. Worktree is CLEAN but has no new commits. OLD — may be abandoned.
- `agent-a700878ae08dda57e` (06-01) — at 0b44315. Task 1 committed (hAtomPoolIds in store + App.tsx). Task 2 UNSTAGED: `src/lib/highlightUtils.ts`, `src/hooks/useKetcherHighlights.ts`, `src/lib/__tests__/highlightUtils.test.ts`.
- `agent-a90ab8d9593954b5b` (06-03) — at 388d87d. COMPLETE. SUMMARY.md exists. Commits: deploy.yml + README.md.
- (Old stale worktrees from previous sessions: agent-a176a9d513b2e4401, agent-a19612cf38e05dca9, agent-a6660fa55ad69aa59, agent-af761372e73423e22 — inspect and clean up if needed)
</current_state>

<completed_work>

**06-03 (GitHub Pages deployment):** COMPLETE in worktree agent-a90ab8d9593954b5b.
- `.github/workflows/deploy.yml` — GitHub Actions CI/CD for gh-pages
- `README.md` — project description, dev setup, deploy instructions
- SUMMARY.md created. 3 commits on branch worktree-agent-a90ab8d9593954b5b.

**06-00 Task 1 (InchiSection test stubs):** COMMITTED to master (59d66d8).
- `src/__tests__/InchiSection.test.tsx` — 5 test cases (2 RED: empty box + placeholder text)
- `src/__tests__/setup.ts` — @testing-library/jest-dom global setup
- `vitest.config.ts` — happy-dom env, globals, setupFiles
- `package.json` — added @testing-library/react, @testing-library/jest-dom, happy-dom

**06-00 Task 2 (H-branch test stubs):** IN WORKING TREE (uncommitted on master).
- `src/lib/__tests__/highlightUtils.test.ts` — +62 lines appended (16x hAtomPoolIds, `describe('INCHI-05: H sub-token highlight')` block)

**06-01 Task 1 (hAtomPoolIds store + App.tsx):** COMMITTED in worktree.
- `src/store.ts` — hAtomPoolIds field added
- `src/App.tsx` — H pool ID collection logic
- Commit 0b44315 on branch worktree-agent-a700878ae08dda57e.
</completed_work>

<remaining_work>

**Immediate (before Wave 2):**

1. **Commit 06-00 Task 2** — The H-branch tests are already in the main working tree as uncommitted changes to `src/lib/__tests__/highlightUtils.test.ts`. Stage and commit them (on master). Then create `06-00-SUMMARY.md` in the phase dir and commit it.

2. **Complete 06-01 Task 2** — In worktree `agent-a700878ae08dda57e`, stage and commit the unstaged changes: `src/lib/highlightUtils.ts`, `src/hooks/useKetcherHighlights.ts`, `src/lib/__tests__/highlightUtils.test.ts`. Then create `06-01-SUMMARY.md`.

3. **Merge Wave 1 worktrees** — After 06-01 Task 2 is committed, merge worktree branches into master (with conflict resolution). Order: 06-01 first (has the implementation), then 06-03 (deploy files, no conflicts expected). The 06-00 worktree is obsolete (its Task 2 was done directly on master) — remove it without merging.

   **Conflict to expect:** `src/lib/__tests__/highlightUtils.test.ts` will conflict between master (Task 2 RED stubs appended) and the 06-01 worktree (which also has its own version of this file). Resolution: keep 06-01's version of the file (which uses the new signatures) since the implementation is there too. After merge the tests should be GREEN.

4. **Also handle master's uncommitted UI changes** — Many source files are modified but uncommitted in the main working tree (Footnote.tsx, Header.tsx, InchiSection.tsx, InchiSection.module.css, KetcherPanel.tsx, KetcherPanel.module.css, styles.css, layerInfo.ts, Explanation.module.css). These are design-handoff changes from prior work. They need to be committed BEFORE or as part of 06-02. The 06-02 plan covers InchiSection empty state — so InchiSection.tsx and InchiSection.module.css changes should be part of 06-02's work.

**Wave 2:**

5. **Execute 06-02** — PLSH-01 implementation + Footnote cleanup.
   - `src/components/InchiSection.tsx` — replace early `return null` with dimmed box
   - `src/components/InchiSection.module.css` — empty-state CSS
   - `src/App.tsx` — remove dead `<Footnote />` node
   Note: Many of these files already have uncommitted changes on master (from design handoff). The 06-02 executor needs to be aware of this — it should BUILD ON the existing uncommitted changes, not overwrite them.

**Post-Wave:**

6. Run phase verification (gsd-verifier)
7. Code review gate
8. Update ROADMAP/STATE (phase complete)
</remaining_work>

<decisions_made>

- Used worktree isolation (USE_WORKTREES=true) for Wave 1 parallel execution
- Spawned 06-00, 06-01, 06-03 as parallel agents in worktrees
- 06-03 completed cleanly; 06-00 and 06-01 stalled on Bash permissions
- 06-00 worktree was reset after agent staged wrong files — the Task 2 H-branch tests appear to have been committed/modified directly on master (possibly from a previous session)
- The stale worktrees (agent-a176a9d513b2e4401 etc.) are from a PRIOR execution attempt of phase 06; they should be inspected and cleaned up
</decisions_made>

<blockers>

- **06-00 Task 2** needs to be committed from the main working tree (already modified, just needs `git add` + `git commit`)
- **06-01 Task 2** needs to be staged + committed in its worktree, then merged
- **Merge conflict** expected on `src/lib/__tests__/highlightUtils.test.ts` between master (RED stubs) and 06-01 worktree (GREEN tests with new signatures) — keep 06-01 version
- **Stale worktrees** from previous sessions need cleanup (or at minimum don't accidentally merge them)
- **Uncommitted UI changes** on master (multiple component files) need to be handled before or during 06-02
</blockers>

## Required Reading (in order)

1. `.planning/phases/06-hydrogen-highlight-polish-and-deploy/06-01-PLAN.md` — understand Task 2 scope (highlightUtils, useKetcherHighlights)
2. `.planning/phases/06-hydrogen-highlight-polish-and-deploy/06-02-PLAN.md` — understand Wave 2 scope (InchiSection empty state, App.tsx Footnote removal)
3. `src/lib/__tests__/highlightUtils.test.ts` — check current content on master (should have 511 lines with H-branch tests at end)
4. `.claude/worktrees/agent-a700878ae08dda57e/src/lib/highlightUtils.ts` — see 06-01 Task 2 implementation (unstaged) before staging

## Critical Anti-Patterns (do NOT repeat these)

- **EXECUTOR OVER-STAGING**: Executor staged 30+ files outside its `files_modified` list. Always verify staged files match plan's `files_modified` before committing.
- **AGENT STALL ON BASH**: Both 06-00 and 06-01 stalled because Bash permission was denied. Always grant Bash permission to executor agents and verify completion via SUMMARY.md + git log spot-checks.

## Infrastructure State

- Master branch: 59d66d8 (test(06-00): add RED InchiSection PLSH-01 empty state tests)
- Active worktrees: agent-a700878ae08dda57e (06-01, partial), agent-a90ab8d9593954b5b (06-03, complete)
- Stale worktrees from prior session: agent-a176a9d513b2e4401, agent-a19612cf38e05dca9, agent-a6660fa55ad69aa59, agent-af761372e73423e22 (inspect before touching)
- Main working tree has uncommitted changes to UI components (design-handoff work)

<context>
We were executing Phase 06 (Wave 1 of 2). Three executor agents were spawned in parallel worktrees. 06-03 completed cleanly. 06-00 and 06-01 stalled on Bash permissions. During investigation we found the main master branch already has the 06-00 Task 1 commit (InchiSection.test.tsx) and the Task 2 changes (H-branch tests in highlightUtils.test.ts) are in the main working tree as uncommitted changes. The 06-01 worktree has Task 1 committed and Task 2 unstaged.

The plan is: commit 06-00 Task 2 from main working tree, complete 06-01 Task 2 in its worktree, merge both worktrees plus 06-03 into master, then run Wave 2 (06-02) as a fresh executor agent.
</context>

<next_action>
Start with: Commit the H-branch test stubs that are already in the main working tree — `git add src/lib/__tests__/highlightUtils.test.ts && git commit -m "test(06-00): add RED H-branch test stubs for INCHI-05"`. Then create 06-00-SUMMARY.md. Then complete 06-01 Task 2 in its worktree.
</next_action>
